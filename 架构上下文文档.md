# AX 架构上下文文档

最后更新：2026-04-27

这份文档只回答一个问题：

如何把 AX 项目的结构压缩成一份稳定、分层、可消费的上下文协议，让 AI 和外部工具更快理解“这个项目是什么、该看哪、能改哪、不能碰哪”。

## 文档定位

这不是新的总规划，也不是新的事实清单。

- [`PLAN.md`](./PLAN.md) 管长期方向、原则、继续条件
- [`WORKLIST.md`](./WORKLIST.md) 管当前施工项
- [`ARCHIVE.md`](./ARCHIVE.md) 管已完成事项归档
- [`PROJECT_FACTS.md`](./PROJECT_FACTS.md) 管当前事实层
- 本文档只管“项目上下文协议”这条设计线

一句话分工：

- `PROJECT_FACTS.md` 回答“AX 现在是什么”
- 本文档回答“AX 要怎样把项目结构变成 AI 可消费的上下文输出”

## 一句话结论

AX 现在不该先做“编译器替你评价架构好不好”，而该先做：

> 让编译器稳定导出项目事实、结构关系、边界信息和局部上下文切片，供 AI 自己做理解、修改和验证决策。

锐评一句：

- “架构规则文件”太容易提前写死设计品味
- “把整个项目全文塞给 AI”太吵、太贵、太不稳定
- “稳定的项目上下文协议”刚好卡在中间，既有工程价值，也符合 AX 当前能力边界

## 为什么值得做

AX 现在已经有：

- `AX.toml + sources`
- `module / import`
- 稳定 diagnostics
- `--json --ai`
- 共享 `foundation`
- project-backed examples

现在这层已经不再是“还没有开始”的设计草案。
`axc context` 已经有七个稳定视图：

- `overview`
- `boundaries`
- `topology`
- `flow`
- `symbol`
- `impact`
- `evidence`

当前真正缺的不是“能不能导出上下文”，而是：

> 如何把这些上下文视图接进 repair / benchmark 消费链，让它们成为 AI 修复输入的一部分。

现在 AI 读项目时常见的问题不是“完全不会读代码”，而是：

- 不知道先看哪几个文件
- 不知道哪个模块是入口，哪个只是 helper
- 不知道哪些地方触碰宿主边界
- 不知道改一个函数会影响哪些模块
- 不知道当前任务只需要哪一小片上下文

所以这条线真正提升的不是“AI 智商”，而是：

- 改代码命中率
- 改动半径控制
- 宿主边界感
- 上下文选择效率

## 不要做成什么

这条线最容易跑歪成下面三种东西：

### 1. 不要做成“编译器架构裁判”

编译器不应该直接输出：

- “这个分层合理”
- “这个模块设计不好”
- “建议你改成某种架构”

原因很简单：

- 这太主观
- 容易把当前作者的偏好写死
- 未来 AI 变强后，这种硬编码判断很快过时

编译器更适合做的是：

- 输出事实
- 输出关系
- 输出边界
- 输出带依据的弱提示

### 2. 不要做成“全量源码打包器”

如果每次都把整个项目源码拼成一坨吐给 AI，会带来几个问题：

- 噪音太大
- Token 成本高
- 不稳定
- AI 容易被无关文件带偏

AX 需要的是压缩后的结构协议，不是另一个 `cat -R`。

### 3. 不要做成“新一份抢权文档”

这条线应该是编译器可导出的机器协议，不是再手写一份会和 `README`、`PROJECT_FACTS.md`、`PLAN.md / WORKLIST.md / ARCHIVE.md` 互相打架的说明书。

## 真正目标

本文建议的目标可以收敛成一句话：

> 把“多文件项目源码”压缩成“稳定的项目结构图 + 关键边界图 + 局部上下文切片”。

这条协议对 AI 最有价值的能力，不是帮它发明架构，而是帮它完成下面几件事：

1. 快速定向
2. 找到正确修改落点
3. 识别危险边界
4. 控制改动影响面
5. 结合已有 diagnostics 做更可控的修复

## 推荐分层

我建议这条协议最终收成六层。

补充说明：

- 下面这套分层是 AX 架构上下文协议的长期完整设计
- 后续即使第一阶段只先落地其中 2-4 层，也不删除这套完整目标
- 也就是说，执行上允许分阶段，设计上保留“大而全六层图景”

一句话：

> 先按阶段交付，不等于放弃完整设计；先做薄，不等于以后不做深。

### 1. 项目总览

回答三个最基础问题：

- 这个项目叫什么
- 入口在哪里
- 模块模式是否开启

推荐字段：

- `project_name`
- `entry`
- `module_mode`
- `source_roots`
- `source_unit_count`
- `core_modules`

这层的作用不是理解细节，而是 3 秒定向。

### 2. 模块职责

这层开始帮助 AI 建立“谁负责什么”的心智模型。

但这里不建议输出绝对判断，而建议输出：

- `role_hints`
- `role_evidence`

例如：

- `entry_orchestrator`
- `shared_foundation`
- `project_library`
- `host_bridge_heavy`
- `diagnostic_surface`

以及对应依据：

- 含 `main`
- import 出度高
- 被多个模块依赖
- 使用宿主 builtin
- 位于 `foundation/`

这样做的好处是：

- 不把角色判断神化
- 允许 AI 根据证据二次判断
- 允许未来协议升级而不改语义立场

### 3. 调用关系

仅有 module/import 还不够，因为很多真实“流程”是沿函数调用走的。

这层建议分两类关系：

- `module_edges`
- `symbol_edges`

最小落地可以先做：

- 模块 import 边
- 函数调用边的静态近似版

这层的作用是让 AI 知道：

- 改一个模块大概会波及谁
- 主流程从哪走到哪
- 哪些函数只是叶子 helper
- 哪些函数是编排点

### 4. 宿主边界

这是 AX 特别值得做硬的一层。

因为 AX 当前一个很大的现实重点，就是把宿主能力暴露成更稳定的可诊断接口。

所以协议里应该明确导出：

- 哪个模块使用了 `fs`
- 哪个模块使用了 `process`
- 哪个模块使用了 `env`
- 哪个模块使用了 `argv`
- 哪个模块有更高风险的 host interaction

推荐字段：

- `host_capabilities`
- `builtin_usage`
- `host_risk_hints`

这层很值钱，因为 AI 在宿主边界最容易乱改、误用和扩大影响面。

### 5. 入口流程

这层回答的问题是：

- 从 `main` 出发，主流程先做什么
- 第一跳到哪
- 核心编排经过哪些函数或模块

第一版不必追求完美全图，只要能给出：

- `entry_symbol`
- `entry_flow`
- `top_level_calls`

就已经比“看一堆文件猜主流程”强很多。

### 6. 证据层

前面 5 层解决的是“看懂什么、改哪里、哪些地方危险”，但如果要真正服务 agent，还需要第 6 层：

- 改完如何验证
- 当前改动应该看哪些 examples / tests / docs / benchmark
- 哪些命令最适合作为最小回归检查

推荐字段：

- `related_examples`
- `related_tests`
- `related_docs`
- `related_benchmarks`
- `recommended_commands`
- `expected_artifacts`

这层的意义不是补更多背景资料，而是把上下文协议从“结构说明”推进到“可执行修改闭环”。

## 更好的形态：不是一个 JSON，而是一组视图

如果只做一个超大 JSON，很快又会变成“另一种信息噪音”。

所以我更推荐把它设计成一组视图。

### 推荐命令形态

```bash
axc context overview <path> --json
axc context topology <path> --json
axc context boundaries <path> --json
axc context flow <path> --json
axc context symbol <path> <symbol> --json
```

后续增强命令：

```bash
axc context impact <path> <symbol> --json
axc context evidence <path> <symbol> --json
```

如果前期不想一次开太大，也可以先做一个最小统一入口：

```bash
axc project <path> --json
```

然后让第一版只输出：

- `overview`
- `source_units`
- `imports`
- `summary`

等这一层站稳后，再拆成多视图。

## 六层与视图的统一映射

为了避免“设计里说六层，命令上又像七八个零散接口”，这里把长期六层和实际视图统一一下：

1. `总览层`
   - 视图：`overview`
   - 作用：给 AI 3 秒定向
2. `结构层`
   - 视图：`topology`
   - 作用：给 AI 看 source unit / module / import / export / 基础 symbol 关系
   - 说明：`role_hints` 与 `role_evidence` 也归在这一层，不单独拆出一层新命令
3. `边界层`
   - 视图：`boundaries`
   - 作用：给 AI 看 `fs / process / env / argv / stdout` 等宿主边界
4. `流程层`
   - 视图：`flow`
   - 作用：给 AI 看入口流程、主调用链和关键分支点
5. `任务切片层`
   - 视图：`symbol` 与 `impact`
   - 作用：给 AI 看“围绕当前改动目标的一圈上下文”和“改动会波及谁”
6. `证据层`
   - 视图：`evidence`
   - 作用：给 AI 看改完怎么验证、该看哪些 example / test / benchmark

一句话：

- 六层是长期设计的语义分层
- `overview / topology / boundaries / flow / symbol / impact / evidence` 是对外视图形态
- 其中 `symbol + impact` 共同承接第 5 层“任务切片”

## 推荐协议形态

下面这份不是最终定稿，而是建议的骨架。

```json
{
  "schema_version": 1,
  "overview": {
    "project_name": "workspace_search_report",
    "entry": "src/main.ax",
    "module_mode": true,
    "source_roots": ["foundation", "lib"]
  },
  "summary": {
    "source_unit_count": 7,
    "module_count": 6,
    "import_edge_count": 9,
    "function_count": 24,
    "type_count": 5
  },
  "modules": [
    {
      "path": "src/main.ax",
      "module_path": null,
      "is_entry": true,
      "imports": ["foundation.cli", "lib.report"],
      "role_hints": ["entry_orchestrator"],
      "role_evidence": ["declares_main", "top_level_call_fanout_high"],
      "exports": ["main"],
      "host_capabilities": [],
      "top_level_calls": ["run_report", "print_summary"]
    },
    {
      "path": "lib/report.ax",
      "module_path": "lib.report",
      "is_entry": false,
      "imports": ["foundation.text"],
      "role_hints": ["project_library", "host_bridge_heavy"],
      "role_evidence": ["uses_fs", "used_by_entry"],
      "exports": ["render_report"],
      "host_capabilities": ["fs_read_dir", "fs_read_to_string"],
      "top_level_calls": []
    }
  ],
  "entry_flow": {
    "entry_symbol": "main",
    "steps": [
      "main",
      "lib.report.render_report"
    ]
  },
  "hotspots": [
    {
      "path": "lib/report.ax",
      "reason": "host_boundary"
    }
  ]
}
```

## 这对 AI 的真实效果是什么

这条线的效果要说实话，不能吹成“AI 看了就会做顶级架构设计”。

它的真实效果更接近：

- 让 AI 更快定位修改入口
- 让 AI 更少误改无关模块
- 让 AI 更容易沿主流程追踪问题
- 让 AI 更容易识别宿主边界和危险改动点
- 让 AI 更容易根据局部上下文做小半径修改

一句话：

它提升的是 AI 的工程命中率和边界感，不是替代人的架构判断力。

## 为什么这比“架构规则文件”更适合先做

当前阶段先做这条线，有几个现实优势：

### 1. 它更贴合 AX 现有实现

AX 已经有：

- `project`
- `module/import`
- `diagnostics`
- `ai feedback`

而“架构规则文件”还需要先定义：

- 规则语法
- 规则解释器
- 规则违例诊断
- benchmark case

那是另一条大工程线。

### 2. 它的主观性更低

项目上下文协议主要输出事实和关系。

架构规则文件则很容易把“当前作者觉得好的写法”强压成编译器立场。

### 3. 它能和未来更强的 AI 兼容

未来 AI 更强时：

- 事实层仍然有价值
- 关系层仍然有价值
- 边界层仍然有价值

但很多今天写死的架构判断，很可能会显得落后。

## 六层完整执行顺序

六层设计不能一口气全做完，否则它会立刻从“上下文协议”变成“新平台工程”。

更合理的方式是：

- 设计上保留完整六层
- 执行上明确分 `P0 -> P1 -> P2`
- 每个阶段只交付最值钱、最容易做稳的一层或两层

### P0：先把锚点和安全网做出来

P0 对应：

- 第 1 层 `overview`
- 第 3 层 `boundaries`
- 附带保守版 `constraint_candidates`

P0 要回答的问题是：

- 这个项目是什么
- 入口在哪
- 哪些 unit 在碰宿主边界
- 哪些低风险边界约束可以被自动候选出来

P0 的目标不是“AI 完整理解架构”，而是：

- 先别迷路
- 先别乱碰危险边界
- 先把第一版 schema 钉成稳定外部契约

P0 推荐产物：

- `axc context overview <path> --json`
- `axc context boundaries <path> --json`
- `facts / hints / validation` 三段式 schema
- `constraint_candidates` 的第一版保守输出
- interface snapshots 与 smoke
- benchmark / repair adapter 的可选 context 输入钩子

### P1：再把结构和局部切片补齐

P1 对应：

- 第 2 层 `topology`
- 第 5 层里的 `symbol`

P1 要回答的问题是：

- 项目的 module / symbol 关系是什么
- 当前要改的 symbol 周围一圈上下文是什么
- 当前改动应该落在哪个文件、哪条边界附近

P1 的目标是把上下文从“安全地图”推进到“可执行局部修改上下文”。

P1 推荐产物：

- `axc context topology <path> --json`
- `axc context symbol <path> <symbol> --json`
- `module_edges / symbol_edges`
- `exports / callers / callees / related_types`
- 更细的 `role_hints` 与 `risk_hints`

### P2：最后补流程、影响和证据闭环

P2 对应：

- 第 4 层 `flow`
- 第 5 层里的 `impact`
- 第 6 层 `evidence`

P2 要回答的问题是：

- 主流程从哪里走到哪里
- 改一个 symbol 会波及谁
- 改完该怎么验证
- 哪些 benchmark / example / test 最该一起看

P2 的目标是把上下文协议从“可读、可导航”推进到“可执行 agent 闭环”。

P2 推荐产物：

- `axc context flow <path> --json`
- `axc context impact <path> <symbol> --json`
- `axc context evidence <path> <symbol> --json`
- `entry_flow / branch_points / recursive_symbols`
- `affected_units / change_risk / likely_breakages`
- `related_examples / related_tests / related_docs / related_benchmarks`

## 第一阶段到长期完整图景的映射

如果把六层和 P0 / P1 / P2 压成一张表，建议这样理解：

1. `总览层 -> overview -> P0`
2. `结构层 -> topology -> P1`
3. `边界层 -> boundaries -> P0`
4. `流程层 -> flow -> P2`
5. `任务切片层 -> symbol + impact -> P1 + P2`
6. `证据层 -> evidence -> P2`

补充说明：

- `constraint_candidates` 不单独算第 7 层
- 它是贯穿 `boundaries` 与 `symbol` 的弱增强输出
- 它的定位不是“正式规则系统”，而是“无需用户先学 DSL 的候选约束”

## 不建议现在就做的东西

下面这些我建议明确后置：

- 编译器直接评价“架构好不好”
- 注解系统
- 宏系统
- 全项目自动重构建议
- 花哨但没有稳定语义依据的“智能摘要”

原因很简单：

- 太主观
- 太容易漂
- 太难 benchmark
- 太容易让协议层失控

## 与现有文档体系的关系

这条线已经进入实现，文档归位保持不变：

- 对外说明仍然优先看 [`README.md`](./README.md)
- 事实层仍然优先看 [`PROJECT_FACTS.md`](./PROJECT_FACTS.md)
- 宿主边界原则仍然优先看 [`docs/host-runtime-boundary.md`](./docs/host-runtime-boundary.md)
- 本文档只作为“上下文协议设计与消费边界”

也就是说：

- 它不替代 `PROJECT_FACTS.md`
- 它不替代 `PLAN.md`
- 它不替代 `WORKLIST.md`
- 它不替代 `ARCHIVE.md`

它只为后续扩展 `axc context`、repair adapter context bundle 和 benchmark context 输入提供设计依据。

## 当前建议

如果你问我现在值不值得继续做，我的判断是：

值得，但下一步重点已经从“先做哪些视图”切换到“哪些视图进入修复链输入”。

这里要明确区分两件事：

- 长期设计：保留完整六层
- 第一阶段执行：先做最值钱、最容易做稳的那几层

当前最合理的执行口径是：

1. `W-P1-01`：把 context 协议接进 repair / benchmark 输入通道
2. `W-P1-02`：定义最小 context 输入壳层，不默认塞全量视图
3. `W-P1-03`：给 context-enabled 路径补 smoke 或接口回归

第一批最适合进入修复输入的视图是：

- `overview`：负责快速定向
- `boundaries`：负责宿主边界安全网
- `evidence`：负责验证建议

也就是说：

- 设计上，保留六层完整方案
- 落地上，先把最小高价值视图接进修复链

一句话结论：

> 六层完整设计保留不删；当前执行重点是让 `overview + boundaries + evidence` 先进入 repair / benchmark 消费链。
