struct Point {
    x: i32,
    y: i32,
}

fn score(point: Point) -> i32 {
    return match (point) {
        Point { x, y } => x + y,
    };
}

fn main() -> i32 {
    let point: Point = Point { x: 20, y: 22 };
    return score(point);
}
