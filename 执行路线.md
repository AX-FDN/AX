# AX 统一执行路线

> 本文件是 AX 当前唯一的执行路线锚点。  
> 旧版计划已经归档到 [`曾经的计划/`](./曾经的计划/)，不再作为当前排期依据。

最后更新：2026-05-02

## 1. 最终目标

AX 的目标不是简单“有一门语言”或“能生成 exe”，而是建立一套适合 AI 自理解、自修复、自验证的语言工具链：

```text
错误分层清楚
  -> AI 知道是哪一层失败
  -> AI 知道该不该改源码
  -> AI 能生成补丁
  -> check / run / build / benchmark 验证修改
  -> 新错误继续分层反馈，直到闭环通过
```

因此 AX 的主目标固定为：

- `axc check` 稳定报告 source / lexer / parser / semantic 错误。
- `axc run` 稳定报告解释执行时的 runtime / host boundary 错误。
- `axc build` 稳定报告 build artifact / AOT readiness / LLVM lowering / toolchain 错误。
- `context` 给 AI 提供项目结构、影响面、证据和建议验证命令。
- benchmark / snapshot 证明 AI 修改前后行为没有漂移。

## 2. 不变原则

- Diagnostic-contract first：先把错误协议做清楚，再扩后端和语法。
- One frontend：解释器、formatter、build/AOT 必须共享同一套 lexer/parser/semantic/HIR/MIR 事实。
- AOT unsupported 不是用户代码错误：合法代码如果解释器能跑但 AOT 不能编，AI 默认不应改用户业务逻辑。
- 每个失败都要能回答：收到什么、做到哪一步、为什么失败、谁应该处理、该跑什么验证。
- 每个新能力都要同时补：实现、结构化反馈、context/build 证据、回归测试、必要文档。

## 3. 错误分层目标

后续所有 diagnostics、build manifest、context evidence 和 benchmark 都要逐步向这套分层靠拢。

| 层 | 代表失败 | AI 默认动作 |
| --- | --- | --- |
| `source_input` | 文件不存在、入口错误、AX.toml 无法读取 | `fix_input_or_config` |
| `lexer` | 非法字符、字符串未闭合 | `edit_source` |
| `parser` | 少括号、少分号、语法结构不完整 | `edit_source` |
| `semantic` | 类型错误、变量未定义、函数参数错误、返回值错误 | `edit_source` |
| `hir_lowering` | AST 合法但无法降到 HIR | `report_compiler_bug` 或 `explain_limitation` |
| `mir_lowering` | HIR 合法但中端控制流/表达式拆分失败 | `report_compiler_bug` 或 `explain_limitation` |
| `interpreter` | 除零、数组越界、host 文件/进程/环境错误 | `edit_source` 或 `fix_runtime_input` |
| `build_artifact` | 输出目录、manifest、artifact 写入失败 | `fix_environment` |
| `aot_readiness` | AOT 子集不支持 string/struct/enum/generic/package | `explain_unsupported` |
| `llvm_lowering` | MIR 进入后端但 LLVM IR 无法生成 | `explain_unsupported` 或 `report_compiler_bug` |
| `toolchain_link` | clang 缺失、链接失败、目标平台问题 | `configure_toolchain` 或 `inspect_toolchain_failure` |
| `internal_compiler` | 编译器内部状态不一致 | `report_compiler_bug` |

统一字段目标：

```json
{
  "layer": "semantic",
  "ai_action": "edit_source",
  "safe_to_edit": true,
  "validation": ["axc check <target>", "axc run <target>"]
}
```

AOT 不支持时的目标形态：

```json
{
  "layer": "aot_readiness",
  "ai_action": "explain_unsupported",
  "safe_to_edit": false,
  "user_code_is_valid": true,
  "interpreter_supported": true,
  "validation": ["axc run <target>", "axc build <target>"]
}
```

### 每层先怎么落地

后续不要同时开很多方向。每一层先按“入口文件 -> 第一刀 -> 验证”的方式推进，先把反馈协议打稳，再扩能力。

| 层 | 主要入口 | 第一刀 | 最小验证 |
| --- | --- | --- | --- |
| `source_input` | `src/cli.rs`、`src/build/input.rs`、project loading | 统一文件不存在、入口错误、项目配置错误的 `layer / ai_action` | `axc check <bad-path> --json --ai`、build input snapshot |
| `lexer` | `src/lexer.rs`、`src/diagnostics.rs` | 给非法字符、字符串未闭合补稳定 `layer=lexer` 与 `ai_action=edit_source` | diagnostics snapshot |
| `parser` | `src/parser.rs`、`src/parser/*` | 给少括号、少分号、语法结构错误补 `safe_to_edit=true` 与验证命令 | `cargo test --lib parser::`、interface snapshot |
| `semantic` | `src/semantic.rs`、`src/semantic/*` | 先覆盖变量未定义、类型不匹配、函数参数错误 | `cargo test --lib semantic::`、repair benchmark case |
| `hir_lowering` | `src/hir.rs`、`src/frontend.rs` | 把 lowering 失败标成 compiler/limitation，不误导 AI 改业务代码 | frontend/HIR 单测 |
| `mir_lowering` | `src/mir.rs`、`src/frontend.rs` | MIR 降级失败必须能区分 unsupported 与 internal bug | MIR 单测、build artifact snapshot |
| `interpreter` | `src/interpreter.rs`、`src/interpreter/*` | runtime/host 错误输出 `fix_runtime_input` 或 `edit_source`，不混成 semantic 错误 | `axc run --json --ai` snapshot |
| `build_artifact` | `src/build/program.rs`、`src/build/model.rs` | manifest/artifact 写入失败进入 build 层，不污染 frontend diagnostics | build snapshot |
| `aot_readiness` | `src/build/readiness.rs` | 所有 AOT 子集缺口输出 blocker，并写明 `source_edit_safe=false` | AOT blocker snapshot |
| `llvm_lowering` | `src/backend/llvm/*` | LLVM IR 生成失败分类为 unsupported/internal，禁止 silent miscompile | backend llvm 单测、IR-only build |
| `toolchain_link` | `src/build/*`、`scripts/smoke-aot-link.ps1`、`scripts/smoke-aot-parity.ps1`、CI | clang/lld/链接失败输出可配置动作，而不是让用户猜环境 | missing clang test、有 clang/parity smoke |
| `internal_compiler` | 全局 `Result` 边界、panic 捕获边界 | 编译器自身状态错误统一标记 `report_compiler_bug` | targeted regression |

## 4. 执行阶段

### G0：路线收口与旧计划归档

目标：让仓库只有一个当前执行路线，旧计划只做历史材料。

必须完成：

- 旧 `PLAN.md`、`WORKLIST.md`、`AOT后端规划.md` 移入 `曾经的计划/`。
- 根目录、docs 索引、事实文档都指向本文件。
- 后续新增计划只改本文件，不再新增并行路线。

### G1：Diagnostic Contract v1

目标：把“AI 应不应该改源码”变成稳定协议，而不是靠自然语言猜。

当前已启动：

- `--json --ai` 的 AI 扩展已开始输出 `layer / ai_action / safe_to_edit / validation`。
- source input / lexer / parser / semantic / interpreter 的代表错误已有首批分层协议。
- 普通输入加载失败已经能在 JSON 模式输出 `I0001`，并在 AI 模式归类为 `source_input / fix_input_or_config`。
- host runtime 输入错误已经能与普通源码修复错误区分，例如缺文件、缺环境变量、argv 越界和进程启动失败会导向 `fix_runtime_input`。
- package resolver 的 `PX****` JSON AI 诊断已归类为 `source_input / fix_input_or_config`。
- HIR/MIR lowering 诊断码 `H****` / `M****` 已归类为 `hir_lowering` / `mir_lowering`，AI 动作为 `report_compiler_bug`，默认不改用户源码。

必须完成：

- 继续扩大 diagnostics 的 `layer` 覆盖面，尤其是后续 build/AOT/toolchain/internal failure。
- 继续扩大 AI 输出的 `ai_action` 覆盖面，避免未知错误被误导成源码修复。
- 为更多高价值错误增加或校准 `safe_to_edit` 和建议验证命令。
- 更新 `docs/diagnostics-schema.md`、`docs/interface-contracts.md` 和快照。
- 保持旧消费者可读，新增字段尽量做 additive 变更。

退出标准：

- source input / lexer / parser / semantic / lowering / runtime 至少各有一类错误暴露明确 `layer` 与 `ai_action`。
- repair benchmark 不需要猜“该改源码还是解释限制”。

### G2：Build/AOT Error Layer v1

目标：让 `axc build` 的失败原因进入 AI 可消费分层。

当前已启动：

- `aot_readiness.schema_version = 3`
- `build-manifest.json` schema version `9`
- AOT blocker 已带 `resolution.agent_action / source_edit_safe / recommended_command`
- AOT blocker 已带 `ai.rule_id / layer / ai_action / safe_to_edit / repair_goal / validation`，并且仍只存在于 build/context 契约，不混入 `check` diagnostics。
- `context evidence` 已不再把 AOT 表述为 `not_started`
- 已有 `examples/aot_return.ax`、`examples/aot_math.ax`、`examples/aot_control_flow.ax`、`examples/aot_loop.ax`、`examples/aot_bool_logic.ax`、`examples/aot_comparisons.ax`、`examples/aot_nested_calls.ax`、`examples/aot_print.ax`、`examples/aot_print_string.ax`、`examples/aot_string_values.ax`、`examples/aot_string_len_compare.ax`、`examples/aot_string_runtime.ax`
- build manifest 已补 `user_code_valid / interpreter_supported / aot_supported`，用于区分“源码已通过前端”“解释器语义可参考”“native AOT 是否真正可用”。
- LLVM lowering unsupported 已用 `AOT2001 / llvm_lowering / explain_unsupported` 表达，和 `AOT1001` clang 缺失、`AOT1002` 链接失败分开。
- `axc build --json` 已确定为打印同一个 build manifest 对象，不新增并行 build summary 协议。
- `scripts/smoke-aot-parity.ps1` 已建立 run vs AOT executable parity helper，默认覆盖 13 个 core/stdout/string/array-read 样例，并比较 `exit code / stdout / stderr`。

下一步必须完成：

- 继续把新增 lowering 先放进 parity smoke，再宣布对应 AOT 能力可用。

退出标准：

- AI 能区分：用户代码错、AOT 子集不支持、clang 缺失、clang 链接失败、编译器内部 bug。
- 缺 clang 不会导致普通 `axc build` 失败，只会稳定输出 `AOT1001` blocker。

### G3：Core AOT Parity

目标：先把极小单文件核心子集做成可验证 AOT，而不是追求大而全。

范围：

- `fn main() -> i32`
- 同文件函数调用
- `i32 / bool`
- `let / assignment / return`
- `if / while` 对应 MIR branch/goto
- 算术、比较、逻辑运算

必须完成：

- 已建立 run vs AOT executable parity helper。
- 有 clang 时运行 exe 并比较 exit code / stdout / stderr。
- 无 clang 时仍能跑 IR-only 测试。
- 已有 13 个 core/stdout/string/array-read 样例通过 `check / run / build`，并在 parity smoke 中对比解释器与 AOT exe。

退出标准：

- `axc run` 与 AOT exe 对核心样例结果一致。
- AOT core 的回归可以在 CI 中稳定证明。

### G4：Runtime ABI 最小闭环

目标：让真实工具程序开始能 AOT，而不是只返回数字。

顺序：

1. 已完成 `println(i32)` / `println(bool)` ABI 第一刀，当前通过 libc `printf` 输出并由 parity smoke 验证 stdout。
2. 已完成 string literal 的只读全局表示，并支持直接 `println("...")`。
3. 已完成最小 string value representation，覆盖 string 局部变量 / 参数 / 返回值。
4. 已完成无 allocator 的 string helper 第一刀，覆盖 `string_len` / `len(string)` 与字符串 `==` / `!=`。
5. 已完成 String Runtime v0，覆盖 `to_string(i32/bool/string)` 与 `string + string`；当前通过 process-lifetime `malloc` 分配，暂不回收。
6. 已完成 Array Read v0，覆盖固定长度数组 literal / 局部变量 / 参数 by value / 索引读取 / `len(array)`；当前主要验证 `[i32; N]`，数组写入和 slice 仍未进入 native layout。
7. 下一步进入 struct layout / field read，再推进 enum 和 match。

必须完成：

- 写清 native string representation 和 ownership 规则。
- runtime helper 链接失败必须进入 `toolchain_link` 或 `runtime_abi` 分层。
- 解释器仍作为语义参考，AOT 不能复制第二套分叉语义。

退出标准：

- 至少一个 hello/report 风格单文件程序可以 AOT。
- runtime ABI 错误不会被误报成用户源码错误。

### G5：Data Layout 与高级语义 AOT

目标：让 AX 的结构化数据进入 native layout。

顺序：

1. 已完成固定数组 read v0，后续补数组写入与 slices。
2. struct layout / field read。
3. enum unit variant。
4. payload enum。
5. match lowering。
6. `Option<T>` / `Result<T,E>`。
7. `?` early-return lowering。

必须完成：

- 每个 layout 都有文档、MIR lowering、LLVM lowering、readiness blocker 更新和 parity 测试。
- unsupported 状态必须稳定输出 blocker，禁止 silent miscompile。

退出标准：

- payload enum + match + Result 样例可以 AOT。
- 当前 project-backed 工具样例的一部分逻辑可被压缩为单文件 AOT smoke。

### G6：Project / Std / Package AOT

目标：让 AOT 从单文件进入真实项目组织。

顺序：

1. 多 source 单项目 AOT。
2. `std.*` AX 源码模块 AOT。
3. local path package AOT。
4. `AX.lock --check` 成为 package AOT 前置验证。
5. build manifest 输出 project/package linking metadata。

退出标准：

- 至少 3 个 project-backed 样例可 AOT。
- 至少 1 个 `std.*` 样例可 AOT。
- 至少 1 个 local path package 项目可 AOT。
- package graph readiness 能在满足条件时变为 AOT-ready。

### G7：AI 自动修复闭环

目标：让 AI 不只是读错误，而是能按协议修复、验证、迭代。

必须完成：

- repair adapter 能消费 `layer / ai_action / safe_to_edit / validation`。
- benchmark case 支持 `check / run / build` 分层验证。
- 对 AOT unsupported case，评分逻辑不奖励“删掉用户逻辑来通过 build”。
- context evidence 能给出最小验证命令集。
- Repair Archaeology 能展示每一步失败属于哪一层、AI 为什么采取这个动作。

退出标准：

- 至少一批 case 能完成：坏代码 -> AI 修复 -> check 通过 -> run 通过 -> build/AOT 通过或正确解释 unsupported。
- benchmark 能证明 AI 反馈没有制造更多误修。

## 5. 当前优先队列

当前只激活下面三件事：

1. 完成 G0：路线收口、旧计划归档、入口引用统一。
2. 推进 G1：全局 diagnostics 的 `layer / ai_action / safe_to_edit / validation` 设计与最小实现。
3. 继续 G2/G3：把 AOT blocker、context evidence、IR-only / clang-link 验证闭环跑稳。

暂不激活：

- JIT
- async
- closure
- trait dispatch AOT
- registry package
- `AX import -> Cargo crate` 直通
- 完整 GC
- 性能宣传 benchmark

## 6. 修改规则

任何新能力进入主线前，必须检查：

- 是否影响 diagnostics JSON。
- 是否影响 AI rule / repair goal / fixit。
- 是否影响 context evidence。
- 是否影响 build manifest / AOT readiness。
- 是否需要 benchmark 或 snapshot。
- 是否需要更新本文件。

如果一项工作不能说明它推进了“AI 理解、修改、验证”闭环，就不进入当前主线。
