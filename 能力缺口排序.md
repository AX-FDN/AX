# AX 能力缺口排序

状态说明：

- `[x]` 已确认
- `[~]` 需要进一步验证
- `[ ]` 暂不推进

## 目的

这份文件用于完成 `P0-26`：

- 把“接下来补什么”从感觉改成有依据的排序
- 把工具风格样例、repair benchmark、当前实现边界放到同一张表里看
- 明确哪些能力值得进入下一批实现，哪些能力现在先不要被带偏

它不替代 [`PLAN.md`](C:/Users/xiaoy/Desktop/A语言/AX/PLAN.md) 的长期路线，也不替代 [`WORKLIST.md`](C:/Users/xiaoy/Desktop/A语言/AX/WORKLIST.md) 的施工清单。

## 证据基线

当前排序基于三类证据：

1. 已落地的 project-backed 真实工具样例：
   [`examples/project_command_capture/`](C:/Users/xiaoy/Desktop/A语言/AX/examples/project_command_capture/)、
   [`examples/project_release_promote/`](C:/Users/xiaoy/Desktop/A语言/AX/examples/project_release_promote/)、
   [`examples/project_directory_index/`](C:/Users/xiaoy/Desktop/A语言/AX/examples/project_directory_index/)、
   [`examples/project_command_batch/`](C:/Users/xiaoy/Desktop/A语言/AX/examples/project_command_batch/)、
   [`examples/project_text_normalize/`](C:/Users/xiaoy/Desktop/A语言/AX/examples/project_text_normalize/)
2. 当前 repair benchmark 清单：
   [`benchmarks/repair-cases.json`](C:/Users/xiaoy/Desktop/A语言/AX/benchmarks/repair-cases.json)
3. 当前共享 AX 基础层与已实现能力边界：
   [`foundation/`](C:/Users/xiaoy/Desktop/A语言/AX/foundation/)、
   [`SYNTAX.md`](C:/Users/xiaoy/Desktop/A语言/AX/SYNTAX.md)、
   [`规划.md`](C:/Users/xiaoy/Desktop/A语言/AX/规划.md)

## 当前判断

AX 现在已经能证明三件事：

- 它不是空壳，已经能承载第一批真实 project 级工具样例
- 它的诊断和 AI 反馈协议已经比“再加一个语法糖”更值钱
- 首轮高价值表达能力已经补得差不多了，当前真正限制下一步的，不是再补一条小语法，而是如何把现有内核基线验实、固实

2026-04-25 的新结论是：

- “更深目录遍历”已经不是抽象猜想了，现有 AX 已经能用递归函数 + 共享 foundation helper 把它在 project 样例里跑通
- 所以它不再是下一编译器主缺口，而更像“样例层 / 共享库层继续推进的能力”
- 代表样例真正开始暴露的新主缺口，是“缺少最小 collections，导致很多工具逻辑只能退化成边遍历边拼字符串”

所以接下来的优先级，不应该按“语言看起来更完整”排序，而应该按“能不能立刻降低代表样例的表达扭曲，并继续提高验证强度”排序。

## 排序结果

### [x] 已完成的首轮高价值能力闭环

这一轮已经落地并应视为当前实现基线：

- 只读切片：`[Type]`、`values[start:end]`、slice 索引读取、数组传 slice 形参
- 最小字符串与格式化能力：`string + string`、`string_len(text)`、`to_string(value)`
- 统一长度查询：`len(value)` 作用于 `string`、固定长度数组和切片
- 空数组字面量策略：只支持显式零长度数组上下文
- 嵌套可写路径：`outer.inner.value = expr;`、`values[index].field = expr;`
- 最小循环提前退出：`break;`

这说明 AX 现在的主瓶颈已经不是“还差哪条小语法能让原型跑起来”，而是“如何证明它真的更适合写工具和更适合给 AI 修复”。

### [x] 已验证：更深目录遍历不是下一编译器主缺口

为什么现在可以降级：

- [`examples/project_directory_index/`](C:/Users/xiaoy/Desktop/A语言/AX/examples/project_directory_index/) 现在已经能递归列出更深层目录
- [`examples/project_workspace_search_report/`](C:/Users/xiaoy/Desktop/A语言/AX/examples/project_workspace_search_report/) 现在已经能递归搜索更深层文件
- 这说明“目录深一层就必须补新语法/新宿主能力”的判断不成立

直接证据：

- [`examples/project_directory_index/lib/scan.ax`](C:/Users/xiaoy/Desktop/A语言/AX/examples/project_directory_index/lib/scan.ax) 已改成递归索引路径
- [`examples/project_workspace_search_report/lib/file_search.ax`](C:/Users/xiaoy/Desktop/A语言/AX/examples/project_workspace_search_report/lib/file_search.ax) 已改成递归搜索路径
- [`tests/interface_snapshots.rs`](C:/Users/xiaoy/Desktop/A语言/AX/tests/interface_snapshots.rs) 已补对应深层 fixture 回归，目录/工作区 project 样例回归已通过

建议收敛范围：

- 继续把“更深遍历”作为样例层能力推进
- 不把它误判成下一个必须先补的编译器大缺口
- 只有当递归写法本身开始明显扭曲代码时，再重新提升它的优先级

### [x] 已落地：最小 collections 第一批

为什么现在排第一：

- 目录索引、文本处理和批处理样例已经开始出现同一类扭曲：没有最小集合能力，只能靠字符串累积、重复扫描或手工流式拼装把逻辑硬撑下去
- 这比“再多走一层目录”更像真正的表达瓶颈
- 如果没有最小 collections，后面很多更真实的工具场景都很难自然落地，比如批量目标聚合、过滤后再输出、排序前暂存、按类别分段汇总

直接证据：

- [`examples/project_directory_index/`](C:/Users/xiaoy/Desktop/A语言/AX/examples/project_directory_index/) 目前仍以“边遍历边输出字符串”的方式维持索引报告，没有真正的动态条目聚合空间
- [`examples/project_text_normalize/`](C:/Users/xiaoy/Desktop/A语言/AX/examples/project_text_normalize/) 目前是单文件归一化，往多文件 / 多规则推进时会首先碰到动态收集问题
- [`examples/project_command_batch/`](C:/Users/xiaoy/Desktop/A语言/AX/examples/project_command_batch/) 目前是固定两步批处理，继续往“命令列表”推进时会先撞上集合能力，而不是 import/module

当前落地结果：

- 2026-04-25 已先以内建 `string_list` 收口第一版，只解决“动态收集字符串条目”这一类最常见工具场景
- 当前已落地 `string_list_new()`、`string_list_push(list, value)`、`string_list_join(list, separator)` 与 `len(string_list)`
- [`examples/project_workspace_search_report/`](C:/Users/xiaoy/Desktop/A语言/AX/examples/project_workspace_search_report/) 已不再依赖纯流式字符串累积来维护 match 明细，而是先收集到 `string_list` 再统一渲染
- [`examples/string_list.ax`](C:/Users/xiaoy/Desktop/A语言/AX/examples/string_list.ax) 与接口/解释器回归测试已经补齐，说明这不是只停在语义层的纸面能力

收敛结论：

- 第一版已经证明“最小 collections”这条线是值钱的
- 但还不应该顺势扩成大而全容器库
- 下一步要观察的，不再是“要不要有最小 collections”，而是“是否还要继续补第二批 collections，还是 `import / module` 更先升级成主阻塞”

### [~] 新第二优先级：`import / module` 进入条件复查

为什么排第二而不是第一：

- 共享 foundation 和 `AX.toml + sources = [...]` 还在继续撑住当前五类样例
- 名字空间冲突和模块边界问题已经开始进入视野，但还没压过集合表达缺口
- 它现在更像“下一批可能升级成主阻塞的组织问题”

直接证据：

- [`foundation/`](C:/Users/xiaoy/Desktop/A语言/AX/foundation/) 已经沉淀 `cli / report / text / search / file_kind / workspace`
- 五类 project-backed 样例都已开始复用共享 helper，而不是完全各写各的
- [`examples/project_*`](C:/Users/xiaoy/Desktop/A语言/AX/examples/) 目前仍然能靠 `sources = ["../../foundation", "lib"]` 组织起来，说明它还没有彻底压过别的缺口

建议收敛范围：

- 继续观察名字冲突、helper 组织和工程边界压力
- 不提前把它拍成“现在就必须上”的大路线
- 让它跟 `minimal collections` 的真实收益继续比较

## 当前明确不应优先的方向

### [x] 现在不该优先：`match`

原因：

- 它能改善表达，但不是当前工具样例或 benchmark 的主瓶颈
- 现有状态机和分支样例仍可被 `if / else` 覆盖
- 它会扩大语法和诊断表面，短期收益不如切片和字符串直接

### [x] 现在不该优先：import / module 真实现

原因：

- 当前 benchmark 和工具样例还主要停留在单文件原型
- 多文件系统会立刻把名字解析、工程结构、诊断和 AI guidance 一起抬复杂
- 现在更适合继续维持稳定 unsupported guidance

### [x] 现在不该优先：脱离真实样例的 native backend 深挖

原因：

- 现阶段主问题不是“能不能出机器码”，而是“AX 能不能稳定承载真实工具程序”
- 在真实样例和最小宿主能力还没站稳前，继续深挖后端不会帮我们判断下一批标准库或语法该补什么

### [x] 现在不该优先：为了好看而补大特性

包括但不限于：

- 泛型
- async
- 异常
- 宏系统
- GUI / Web 叙事

原因：

- 这些方向都更容易带来范围膨胀，而不是直接提高当前主线指标

## 下一批实现顺序

建议按这个顺序进入后续实施：

当前状态补充：
- 2026-04-24 已补齐第一版嵌套可写路径，当前支持 `outer.inner.value = expr;` 与 `values[index].field = expr;`
- 2026-04-24 已补齐第一版最小循环提前退出，当前支持 `break;`，可替换工具样例里的机械退场写法
- 2026-04-25 已落下五类 project-backed 真实工具样例，并形成第一版共享 AX 基础层
- 2026-04-25 已在 project 样例中验证递归目录遍历与递归搜索，说明更深目录遍历可继续沿现有 AX 能力推进
- 下一步应先补最小 collections，再继续观察 import/module 是否升级成新的主阻塞

1. 扩 full repair benchmark 与 compare replay 基线
2. 固化现有五类 project-backed 样例为代表集
3. 把代表样例接进更硬的 `check / run / build / smoke`
4. 先补最小 collections 第一批
5. 再继续复查 `import / module` 是否已经压过别的缺口

如果其中某一项迟迟拿不稳，就先冻结为稳定边界 + 明确 guidance，不要拖着整批能力一起悬空。

## 一句话结论

AX 的下一批重点，不该优先补“更像一门完整语言”的东西，而该优先补“更像一套能被 AI 稳定写、稳定修、还能承载真实工具程序的工程系统”的东西。
