# AX 能力缺口排序

状态说明：

- `[x]` 已确认
- `[~]` 需要进一步验证
- `[ ]` 暂不推进

## 目的

这份文件用于完成 `P0-26`：

- 把“接下来补什么”从感觉改成有依据的排序
- 把工具风格样例、repair benchmark、当前实现边界放到同一张表里看
- 明确哪些能力值得进入下一批实现，哪些能力现在先不要被带偏

它不替代 [`PLAN.md`](C:/Users/xiaoy/Desktop/A语言/AX/PLAN.md) 的长期路线，也不替代 [`WORKLIST.md`](C:/Users/xiaoy/Desktop/A语言/AX/WORKLIST.md) 的施工清单。

## 证据基线

当前排序基于三类证据：

1. 已落地的工具风格样例：
   [`bootstrap_token_scan.ax`](C:/Users/xiaoy/Desktop/A语言/AX/examples/bootstrap_token_scan.ax)、
   [`bootstrap_state_machine.ax`](C:/Users/xiaoy/Desktop/A语言/AX/examples/bootstrap_state_machine.ax)、
   [`bootstrap_block_summary.ax`](C:/Users/xiaoy/Desktop/A语言/AX/examples/bootstrap_block_summary.ax)
2. 当前 repair benchmark 清单：
   [`benchmarks/repair-cases.json`](C:/Users/xiaoy/Desktop/A语言/AX/benchmarks/repair-cases.json)
3. 当前已实现能力边界：
   [`SYNTAX.md`](C:/Users/xiaoy/Desktop/A语言/AX/SYNTAX.md)、
   [`规划.md`](C:/Users/xiaoy/Desktop/A语言/AX/规划.md)

## 当前判断

AX 现在已经能证明三件事：

- 它不是空壳，已经能承载小型工具式逻辑
- 它的诊断和 AI 反馈协议已经比“再加一个语法糖”更值钱
- 真正限制下一批样例和 benchmark 的，不是 `match` 这类大特性，而是遍历、字符串和数据输入输出的实用能力

所以接下来的优先级，不应该按“语言看起来更完整”排序，而应该按“能不能立刻扩充真实工具样例、提高 benchmark 代表性、且不明显伤害性能和稳定性”排序。

## 排序结果

### [x] 第一优先级：切片与更合理的遍历能力

为什么排第一：

- 当前工具样例几乎都被固定长度数组绑死，函数签名只能写成 `[Token; 6]`、`[TokenKind; 10]` 这种硬编码形式
- 同一类扫描逻辑很难复用到不同长度输入
- 真正的工具代码离不开“给一段序列做扫描、过滤、汇总”，而不是只处理编译期写死长度的数组

直接证据：

- [`bootstrap_token_scan.ax`](C:/Users/xiaoy/Desktop/A语言/AX/examples/bootstrap_token_scan.ax) 里统计逻辑被数组长度常量锁死
- [`bootstrap_state_machine.ax`](C:/Users/xiaoy/Desktop/A语言/AX/examples/bootstrap_state_machine.ax) 和 [`bootstrap_block_summary.ax`](C:/Users/xiaoy/Desktop/A语言/AX/examples/bootstrap_block_summary.ax) 也都需要手写固定长度与结束哨兵
- [`规划.md`](C:/Users/xiaoy/Desktop/A语言/AX/规划.md) 已把“切片路线”列在 P1 最前

建议收敛范围：

- 先做只读切片
- 先服务数组遍历和函数参数传递
- 不在这一批顺手引入复杂借用系统或泛型叙事

当前状态更新：
- 已于 2026-04-24 完成第一版只读切片闭环，当前支持 `[Type]`、`values[start:end]`、slice 索引读取与数组传 slice 形参。
- 接下来的第一优先级顺延为“更贴近工具代码的遍历能力”。

### [x] 第二优先级：更实用的字符串处理

为什么排第二：

- `PLAN.md` 里的目标任务有大量 CLI、文件处理、文本处理、日志摘要
- 当前样例能做控制流和数据结构，但还不能自然地写真正有用的文本工具
- 如果没有更实用的字符串能力，AX 很难从“会跑几个小程序”走向“能写真实工具”

直接证据：

- [`PLAN.md`](C:/Users/xiaoy/Desktop/A语言/AX/PLAN.md) 的 benchmark 任务含文本处理、目录遍历、日志摘要、代码生成工具
- 当前工具样例几乎全部回避了真实文本操作
- [`规划.md`](C:/Users/xiaoy/Desktop/A语言/AX/规划.md) 里也已经把“更实用的字符串处理”放在 P1

建议收敛范围：

- 先补最小可用字符串能力
- 目标是支撑扫描、拼接、简单比较、简单格式输出
- 不把这一批扩成完整 Unicode 字符串设计工程

当前状态更新：
- 已于 2026-04-24 完成第一版字符串能力闭环，当前支持 `string + string` 与 `string_len(text)`。
- 接下来的第一优先级顺延为“更贴近工具代码的遍历能力”。

### [x] 第三优先级：空数组字面量策略

为什么排第三：

- 它已经是 repair benchmark 里的稳定修复 case
- 这是模型很容易自然写出来的代码形态
- 如果不尽快决定边界，后续 AI 反馈和 benchmark 口径会一直悬着

直接证据：

- [`benchmarks/repair-cases.json`](C:/Users/xiaoy/Desktop/A语言/AX/benchmarks/repair-cases.json) 里已有 `empty_array_literal_unsupported`
- [`规划.md`](C:/Users/xiaoy/Desktop/A语言/AX/规划.md) 和 [`WORKLIST.md`](C:/Users/xiaoy/Desktop/A语言/AX/WORKLIST.md) 都已把这件事单独列出来

建议收敛范围：

- 已于 2026-04-24 收敛为“只支持显式零长度数组上下文”
- 合法写法示例：`let values: [i32; 0] = [];`
- 非零长度上下文继续稳定报 `S0032`
- 不继续向通用推导扩张，先保持规则唯一和行为稳定

### [x] 第四优先级：更贴近工具代码的遍历与集合能力

为什么排第四：

- 切片和字符串补完后，真正的下一个瓶颈会是“怎样更自然地表达扫描、计数、分组、累积”
- 当前可以写循环，但表达密度还不够，很多逻辑会显得机械而难复用

直接证据：

- 三个工具样例都大量依赖手写下标循环
- [`规划.md`](C:/Users/xiaoy/Desktop/A语言/AX/规划.md) 里已经把“更贴近工具链代码的遍历能力”列入 P1

建议收敛范围：

- 先围绕已有数组/切片补最小遍历能力
- 先不引入大而全 collections 设计

当前状态更新：
- 已于 2026-04-24 完成第一版最小遍历能力闭环，当前支持统一 `len(value)`，可对 `string`、固定长度数组和切片做长度查询。
- 接下来的第一优先级顺延为“更多工具风格坏例子与修复样例”。

## 当前明确不应优先的方向

### [x] 现在不该优先：`match`

原因：

- 它能改善表达，但不是当前工具样例或 benchmark 的主瓶颈
- 现有状态机和分支样例仍可被 `if / else` 覆盖
- 它会扩大语法和诊断表面，短期收益不如切片和字符串直接

### [x] 现在不该优先：import / module 真实现

原因：

- 当前 benchmark 和工具样例还主要停留在单文件原型
- 多文件系统会立刻把名字解析、工程结构、诊断和 AI guidance 一起抬复杂
- 现在更适合继续维持稳定 unsupported guidance

### [x] 现在不该优先：native backend 深挖

原因：

- 现阶段主问题不是“能不能出机器码”，而是“语言面是否真的适合 AI 写工具”
- 在能力缺口还没收敛前，继续深挖后端不会帮我们判断下一批语法该补什么

### [x] 现在不该优先：为了好看而补大特性

包括但不限于：

- 泛型
- async
- 异常
- 宏系统
- GUI / Web 叙事

原因：

- 这些方向都更容易带来范围膨胀，而不是直接提高当前主线指标

## 下一批实现顺序

建议按这个顺序进入后续实施：

1. 更多工具风格坏例子与修复样例
2. 围绕数据结构与格式化支撑继续补最小高价值能力

如果其中某一项迟迟拿不稳，就先冻结为稳定边界 + 明确 guidance，不要拖着整批能力一起悬空。

## 一句话结论

AX 的下一批能力，不该优先补“更像一门完整语言”的东西，而该优先补“能让它更像一门真实可写工具的 AI-first 语言”的东西。
