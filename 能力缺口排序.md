# AX 能力缺口排序

状态说明：

- `[x]` 已确认
- `[~]` 需要进一步验证
- `[ ]` 暂不推进

## 目的

这份文件用于完成 `P0-26`：

- 把“接下来补什么”从感觉改成有依据的排序
- 把工具风格样例、repair benchmark、当前实现边界放到同一张表里看
- 明确哪些能力值得进入下一批实现，哪些能力现在先不要被带偏

它不替代 [`PLAN.md`](C:/Users/xiaoy/Desktop/A语言/AX/PLAN.md) 的长期路线，也不替代 [`WORKLIST.md`](C:/Users/xiaoy/Desktop/A语言/AX/WORKLIST.md) 的施工清单。

## 证据基线

当前排序基于三类证据：

1. 已落地的工具风格样例：
   [`bootstrap_token_scan.ax`](C:/Users/xiaoy/Desktop/A语言/AX/examples/bootstrap_token_scan.ax)、
   [`bootstrap_state_machine.ax`](C:/Users/xiaoy/Desktop/A语言/AX/examples/bootstrap_state_machine.ax)、
   [`bootstrap_block_summary.ax`](C:/Users/xiaoy/Desktop/A语言/AX/examples/bootstrap_block_summary.ax)
2. 当前 repair benchmark 清单：
   [`benchmarks/repair-cases.json`](C:/Users/xiaoy/Desktop/A语言/AX/benchmarks/repair-cases.json)
3. 当前已实现能力边界：
   [`SYNTAX.md`](C:/Users/xiaoy/Desktop/A语言/AX/SYNTAX.md)、
   [`规划.md`](C:/Users/xiaoy/Desktop/A语言/AX/规划.md)

## 当前判断

AX 现在已经能证明三件事：

- 它不是空壳，已经能承载小型工具式逻辑
- 它的诊断和 AI 反馈协议已经比“再加一个语法糖”更值钱
- 首轮高价值表达能力已经补得差不多了，当前真正限制下一批样例和 benchmark 的，是 benchmark 代表性、最小工具宿主能力和真实工具样例

所以接下来的优先级，不应该按“语言看起来更完整”排序，而应该按“能不能立刻扩充真实工具样例、提高 benchmark 代表性、且不明显伤害性能和稳定性”排序。

## 排序结果

### [x] 已完成的首轮高价值能力闭环

这一轮已经落地并应视为当前实现基线：

- 只读切片：`[Type]`、`values[start:end]`、slice 索引读取、数组传 slice 形参
- 最小字符串与格式化能力：`string + string`、`string_len(text)`、`to_string(value)`
- 统一长度查询：`len(value)` 作用于 `string`、固定长度数组和切片
- 空数组字面量策略：只支持显式零长度数组上下文
- 嵌套可写路径：`outer.inner.value = expr;`、`values[index].field = expr;`
- 最小循环提前退出：`break;`

这说明 AX 现在的主瓶颈已经不是“还差哪条小语法能让原型跑起来”，而是“如何证明它真的更适合写工具和更适合给 AI 修复”。

### [~] 新第一优先级：更多工具风格坏例子与 benchmark 代表性

为什么排第一：

- 当前 smoke 证据链已经稳了，但 full repair benchmark 还不够厚
- compare replay 资产还主要围着 smoke 子集组织，不足以支撑更强的外部说服力
- 如果 benchmark 代表性不够，后面不管是补标准库还是补 backend，都很容易变成拍脑袋

直接证据：

- [`benchmarks/repair-cases.json`](C:/Users/xiaoy/Desktop/A语言/AX/benchmarks/repair-cases.json) 当前 full case 数量和 tool-style 覆盖面仍有限
- compare replay 目录目前仍然偏向 smoke 基线证明，而不是 full compare 复跑
- [`规划.md`](C:/Users/xiaoy/Desktop/A语言/AX/规划.md) 当前主线已经转向“先把证据链做厚”

建议收敛范围：

- 先吃掉仓库中已经存在、但尚未纳入 manifest 的现成坏例子
- 先把 full compare shared replay 资产补起来
- 不把 smoke 子集和 full 基线混成一套口径

### [~] 新第二优先级：最小工具宿主能力 / 标准库第一批

为什么排第二：

- AX 现在已经能写小型玩具逻辑，但还缺“真正有用的工具程序”离不开的宿主能力
- `PLAN.md` 的目标任务是 CLI、文件处理、构建辅助、文本处理，不是只有数组扫描和状态机
- 如果没有 `process / env / path / fs / string` 这类最小面，后续真实样例和 backend 验证都没有载体

直接证据：

- [`PLAN.md`](C:/Users/xiaoy/Desktop/A语言/AX/PLAN.md) 明确把 CLI / 编译器 / 构建工具 / 自动化工具列为第一目标场景
- 当前 examples 仍然主要停留在纯内存数据和手写数组输入
- [`WORKLIST.md`](C:/Users/xiaoy/Desktop/A语言/AX/WORKLIST.md) 已把最小工具宿主能力第一批提升为下一轮待做项

建议收敛范围：

- 先补最小宿主能力，不先做大而全标准库
- 每补一个能力，都同步补误用 diagnostics、AI guidance、示例和测试
- 不在这一批把 import/module、泛型或宏系统一起拉进来

### [~] 新第三优先级：第二组真实工具样例与 build 验证目标

为什么排第三：

- `axc build` 现在最大的风险不是“没有骨架”，而是“没有足够像样的验证对象”
- 如果没有第二组更接近真实任务的样例，后续 backend 很容易证明不了路线，只能证明能导出点东西
- 新语法到底值不值，也应该由这些样例反过来证明

直接证据：

- 当前高价值 examples 主要是 token 扫描、状态机、block summary、traversal、format_report、token_rewrite 这类原型级样例
- [`规划.md`](C:/Users/xiaoy/Desktop/A语言/AX/规划.md) 已把 backend 推进条件改成“先有真实工具样例和证据”

建议收敛范围：

- 至少再补 2-4 个更像 CLI / 文本处理 / 构建辅助 / 批处理的样例
- 这些样例先要能 `check / run`
- 再把它们当成后续 `axc build` 的第一批验证目标

## 当前明确不应优先的方向

### [x] 现在不该优先：`match`

原因：

- 它能改善表达，但不是当前工具样例或 benchmark 的主瓶颈
- 现有状态机和分支样例仍可被 `if / else` 覆盖
- 它会扩大语法和诊断表面，短期收益不如切片和字符串直接

### [x] 现在不该优先：import / module 真实现

原因：

- 当前 benchmark 和工具样例还主要停留在单文件原型
- 多文件系统会立刻把名字解析、工程结构、诊断和 AI guidance 一起抬复杂
- 现在更适合继续维持稳定 unsupported guidance

### [x] 现在不该优先：脱离真实样例的 native backend 深挖

原因：

- 现阶段主问题不是“能不能出机器码”，而是“AX 能不能稳定承载真实工具程序”
- 在真实样例和最小宿主能力还没站稳前，继续深挖后端不会帮我们判断下一批标准库或语法该补什么

### [x] 现在不该优先：为了好看而补大特性

包括但不限于：

- 泛型
- async
- 异常
- 宏系统
- GUI / Web 叙事

原因：

- 这些方向都更容易带来范围膨胀，而不是直接提高当前主线指标

## 下一批实现顺序

建议按这个顺序进入后续实施：

当前状态补充：
- 2026-04-24 已补齐第一版嵌套可写路径，当前支持 `outer.inner.value = expr;` 与 `values[index].field = expr;`
- 2026-04-24 已补齐第一版最小循环提前退出，当前支持 `break;`，可替换工具样例里的机械退场写法
- 下一步应先补 benchmark 代表性、最小宿主能力和真实工具样例，而不是跳去做大语法面

1. 扩 full repair benchmark 与 compare replay 基线
2. 补最小工具宿主能力第一批
3. 用新能力落第二组真实工具样例
4. 再决定下一步更值的是 backend、标准库扩张还是某个高价值语法点

如果其中某一项迟迟拿不稳，就先冻结为稳定边界 + 明确 guidance，不要拖着整批能力一起悬空。

## 一句话结论

AX 的下一批重点，不该优先补“更像一门完整语言”的东西，而该优先补“更像一套能被 AI 稳定写、稳定修、还能承载真实工具程序的工程系统”的东西。
