# AX 最小可写工具内核

最后更新：2026-04-25

这个文件的目的只有一个：

- 把 AX 当前 B 线的目标收窄成一个明确、可执行、可协作的交付口径
- 让并行开发时，大家知道这条线现在优先补什么、不优先补什么
- 降低“语言能力实现”和“AI 修复协议 / benchmark 证据链”互相踩文件的概率

## 文档定位

这不是新的总路线文档，也不替代 [`PLAN.md`](./PLAN.md) 或 [`WORKLIST.md`](./WORKLIST.md)。

- `PLAN.md` 管方向、原则、边界和继续条件
- `WORKLIST.md` 管当前事项、状态和完成记录
- 本文件只管当前这条线的目标定义、协作边界和执行顺序

一句话：

- `PLAN.md` 回答“为什么做”
- `WORKLIST.md` 回答“最近在做什么”
- 本文件回答“最小可写工具内核这条线现在到底算什么、先补什么、怎么避免冲突”

## 一句话定义

AX 当前不是要先补成“一门常见语法大而全的语言”，而是要先补成：

> 一门已经能比较自然地写出一批真实小工具的语言原型。

这里的“最小可写工具内核”指的是四件东西一起成立：

1. 语言本体已经足够表达真实小工具的核心控制流和数据组织
2. 宿主能力已经足够支撑 `CLI / 文本处理 / 工作区扫描 / 构建辅助` 这类任务
3. AX 侧已经有一层共享基础库，而不是每个示例都各自重复拼 helper
4. 多文件组织已经足够稳定，不会让示例一变大就立刻散掉

## 当前阶段的真实目标

当前阶段不要追求“把大多数人常用语法一次性补齐”。

当前阶段真正该追求的是：

- 让 AX 能稳定写出 `3-5` 类真实小工具
- 让这些工具不再依赖大量重复 helper
- 让这些工具能用共享 foundation 组织，而不是只靠单文件硬撑
- 让“下一步该补语法还是该补库”由真实样例暴露，而不是靠感觉拍脑袋

## 当前定义下，什么算“内核已到位”

满足下面这些条件，才算“最小可写工具内核”基本成形：

- 已有一批真实工具风格样例可以自然 `check / run`
- 这些样例覆盖至少三类任务：
  - CLI 参数与输出整理
  - 文本处理与报告生成
  - 工作区 / 目录 / 文件辅助任务
- AX 侧共享 foundation 已经开始替代示例里的重复 helper
- 多文件项目组织已经不再明显拖后腿
- 新增宿主能力时，不需要每次都大面积改示例结构

更直白地说：

- 到这个点，AX 还不是成熟语言
- 但它已经不再只是“能跑玩具样例”的原型

## 当前最值得补的东西

### 1. 共享 AX 基础层

优先把已经在多个示例里反复出现的逻辑收成共享 foundation。

当前最值得先收的是：

- `foundation/text`
- `foundation/report`
- `foundation/cli`
- `foundation/workspace`

标准很简单：

- 如果一个 helper 在两个以上真实样例里重复出现，就不应该继续散落在各自项目里

### 2. 第一批宿主能力

优先级最高的不是更花的语法，而是写真实工具必需的宿主能力。

当前第一批默认范围：

- `io`
- `fs`
- `path`
- `env`
- `process`
- `string`

判断标准：

- 没有它，真实小工具就写不顺
- 加上它之后，马上能多出一个更像真实工具的样例
- 它属于宿主原语，而不是高层业务流程拼装

### 3. 稳定的多文件组织

当前不要求立刻把完整包系统、跨仓库分发和生态全做完。

但必须继续把下面这件事做顺：

- 一个稍微像样的 AX 工具项目，能够稳定拆成多文件，并有清楚的共享代码入口

当前默认路线：

- 优先继续打磨现有 `AX.toml` + `sources = [...]` 组织方式
- 只有当真实样例已经持续暴露痛点时，才推进语言级 `import / module`

### 4. 真实工具样例

每补一项能力，必须带一个更像真实工作的样例，而不是只补语法点。

当前样例的价值标准不是“语法展示得多全”，而是：

- 它是不是一个真实会被拿来写的工具
- 它是不是暴露了下一个真正值得补的缺口

## 当前不应该优先补的东西

下面这些东西不是永远不做，但不应该压过“最小可写工具内核”主线：

- `match`
- 泛型
- 宏
- `async / await`
- 异常系统
- 复杂包系统
- 通用 FFI
- `AX import -> Cargo crate` 桥接
- 官方 `network / concurrency` 标准库承诺
- 围绕某一代模型 tokenizer 的表面优化

原因很简单：

- 这些东西表面积大
- 它们会快速放大语义、解释器、diagnostics、文档和测试负担
- 它们很容易把项目从“证明价值”带偏成“先堆语言面”

## 当前协作边界

这条线当前建议负责的文件：

- [`src/semantic/checker/builtin.rs`](./src/semantic/checker/builtin.rs)
- [`src/interpreter.rs`](./src/interpreter.rs)
- [`src/semantic.rs`](./src/semantic.rs)
- [`src/parser.rs`](./src/parser.rs)
- [`src/lexer.rs`](./src/lexer.rs)
- [`src/hir.rs`](./src/hir.rs)
- [`src/mir.rs`](./src/mir.rs)
- [`src/project.rs`](./src/project.rs)
- [`src/build.rs`](./src/build.rs)
- [`examples/`](./examples/)
- [`SYNTAX.md`](./SYNTAX.md)
- [`docs/host-runtime-boundary.md`](./docs/host-runtime-boundary.md)

这一线当前不建议主动改：

- [`src/ai.rs`](./src/ai.rs)
- [`benchmarks/repair-cases.json`](./benchmarks/repair-cases.json)
- [`benchmarks/repair-cases-smoke.json`](./benchmarks/repair-cases-smoke.json)
- [`benchmarks/repair-candidates/`](./benchmarks/repair-candidates/)
- [`scripts/export-repair-benchmark.ps1`](./scripts/export-repair-benchmark.ps1)
- [`scripts/run-repair-benchmark.ps1`](./scripts/run-repair-benchmark.ps1)
- [`scripts/score-repair-benchmark.ps1`](./scripts/score-repair-benchmark.ps1)
- [`scripts/compare-repair-feedback.ps1`](./scripts/compare-repair-feedback.ps1)
- [`scripts/compare-repair-modes.ps1`](./scripts/compare-repair-modes.ps1)
- [`scripts/smoke-compare-repair-feedback.ps1`](./scripts/smoke-compare-repair-feedback.ps1)
- [`scripts/smoke-compare-repair-modes.ps1`](./scripts/smoke-compare-repair-modes.ps1)
- [`scripts/smoke-repair-benchmark.ps1`](./scripts/smoke-repair-benchmark.ps1)
- [`docs/repair-benchmark.md`](./docs/repair-benchmark.md)
- [`docs/repair-adapter-spec.md`](./docs/repair-adapter-spec.md)
- [`docs/diagnostics-schema.md`](./docs/diagnostics-schema.md)

## 当前高冲突区

下面这些文件如果多人同时改，很容易冲突或口径漂移：

- [`README.md`](./README.md)
- [`PLAN.md`](./PLAN.md)
- [`WORKLIST.md`](./WORKLIST.md)
- [`规划.md`](./规划.md)
- [`tests/interface_snapshots.rs`](./tests/interface_snapshots.rs)

处理原则：

- 不在每个小提交里都顺手改这些文件
- 只有当一个里程碑真正收口时，再集中同步
- 如果只是新增宿主能力或示例，优先把变更收在实现和样例里，不要顺带改全局文档

## 这条线的默认交付顺序

新增一项能力时，默认按下面顺序落地：

1. 先补 checker / interpreter / 必要前端实现
2. 再补一个 AX-facing 的共享 foundation 封装或 helper
3. 再补一个真实工具样例
4. 再补用户可见文档或语法说明
5. 最后再决定要不要把它接进 repair / benchmark 资产

这个顺序的原因很简单：

- 先让能力真正可用
- 再让能力真正可复用
- 再让能力暴露真实价值
- 最后才让协议和 benchmark 去跟上真实事实

## 这条线当前推荐的施工批次

### 第一批：收共享 foundation

优先目标：

- 把已有样例里的重复 text / report / cli 逻辑抽出来
- 让多个项目样例先用同一套 AX 侧 helper

这一批的成功标志：

- 同类样例不再各写一份 `report`、`text_stats`、`cli args` 解析

### 第二批：补第一轮宿主能力

优先目标：

- 把 `fs / path / env / process / string / io` 真正补到“够写工具”

这一批的成功标志：

- 真实样例不再主要靠纯玩具输入驱动
- 至少一批项目样例已经明显比单文件演示更接近真实工作

### 第三批：判断多文件组织是否仍然卡住

优先目标：

- 先用现有 `AX.toml` 组织路线继续推进
- 只有当真实样例已经持续证明“没有语言级导入就非常别扭”时，才推进下一步

这一批的成功标志：

- 可以明确回答：
  - 现有项目组织够不够继续撑下一批工具样例
  - 现在最值钱的缺口到底是 `import/module`，还是别的能力

## 暂不拍板的开放问题

下面这些问题可以讨论，但当前不应提前拍板：

- `import / module` 到底是近期必须项，还是 `AX.toml` 还能继续撑一段
- `continue` 是否真的比补库更值
- 最小 `collections` 到底要做到哪
- 什么时候从“共享 foundation”升级成真正的官方标准库层

处理原则：

- 先让真实样例把问题暴露出来
- 再决定是补语法、补库，还是继续沿现有组织方式推进

## 变更前自检清单

准备改这条线时，先过一遍下面这几条：

1. 这次改动是在补“最小可写工具内核”，还是在扩大语言表面积？
2. 没有这项能力，现有真实工具样例是否真的写不顺？
3. 这次改动是否会顺手踩到 A 线文件？
4. 这次改动是否需要同步全局文档，还是可以先收在局部实现和样例里？
5. 这项能力落地后，能不能立刻配一个更真实的样例？

只要上面五条里有两条答不上来，就先不要动大面。

## 当前一句话结论

当前这条线的正确目标不是：

- “把常见语法先补得差不多”

而是：

- “把 AX 先补到足够自然地写出一批真实小工具，然后再决定下一批最值钱的语法和标准库能力”

更直接一点：

- 现在先补“工具可写性”
- 不是先补“语言表面积”
