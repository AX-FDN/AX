# AX 最小可写工具内核

最后更新：2026-04-25

这个文件的目的只有一个：

- 把 AX 当前 B 线的目标收敛成一个明确、可执行、可协作的交付口径
- 让并行开发时，大家知道这条线现在优先补什么、不优先补什么
- 降低“语言能力实现”和“AI 修复协议 / benchmark 证据链”互相踩文件的概率

## 文档定位

这不是新的总路线文档，也不替代 [`PLAN.md`](./PLAN.md) 或 [`WORKLIST.md`](./WORKLIST.md)。

- `PLAN.md` 管方向、原则、边界和继续条件
- `WORKLIST.md` 管当前事项、状态和完成记录
- 本文件只管“最小可写工具内核”这条线当前已经到哪、还差什么、接下来怎么验

一句话：

- `PLAN.md` 回答“为什么做”
- `WORKLIST.md` 回答“最近在做什么”
- 本文件回答“最小可写工具内核这条线现在到底算什么、接下来先验什么、怎么避免冲突”

## 一句话定义

AX 当前不是要先补成“一门常见语法大而全的语言”，而是要先证明：

> 现有语言内核是否已经足够稳定地写出一组真实小工具，并能用这组工具暴露下一批真正值得补的缺口。

这里的“最小可写工具内核”不再只是“待建设的目标”，而是一个正在进入验收阶段的基线。

## 当前判断

这条线现在已经不处于“刚起步、先把 foundation 和宿主能力补起来”的阶段了。

更准确的判断是：

- 第一版共享 AX 基础层已经成形
- 第一批最小宿主能力已经被真实 project 样例压过一轮
- `AX.toml + sources = [...]` 的多文件组织路线已经基本跑通
- 下一步不该继续像启动期一样无节制补样例品类
- 下一步应该转向“固化现有基线，并让现有样例逼出下一个缺口”

## 当前已经成立的事实

下面这些事情，按当前仓库状态，已经不是“计划”，而是“事实”：

### 1. 共享 foundation 第一版已经形成

当前 repo 根目录 [`foundation/`](./foundation/) 已有：

- [`foundation/cli.ax`](./foundation/cli.ax)
- [`foundation/file_kind.ax`](./foundation/file_kind.ax)
- [`foundation/report.ax`](./foundation/report.ax)
- [`foundation/search.ax`](./foundation/search.ax)
- [`foundation/text.ax`](./foundation/text.ax)
- [`foundation/workspace.ax`](./foundation/workspace.ax)

这说明 AX 侧共享基础层已经不再只是想法，而是第一版可复用事实。

### 2. 已经落下五类 project-backed 真实工具样例

当前至少已有下面五类 project 样例：

- [`examples/project_command_capture/`](./examples/project_command_capture/)
- [`examples/project_release_promote/`](./examples/project_release_promote/)
- [`examples/project_directory_index/`](./examples/project_directory_index/)
- [`examples/project_command_batch/`](./examples/project_command_batch/)
- [`examples/project_text_normalize/`](./examples/project_text_normalize/)

这五类样例分别覆盖了：

- `process / env / path / fs`
- release/promote 文件链路
- 目录索引链路
- 批处理命令编排链路
- 文本重写链路

所以“让 AX 稳定写出 `3-5` 类真实小工具”这件事，已经不是未来口号，而是接近完成的当前事实。

### 3. 多文件组织第一版已经过关

当前 `AX.toml + sources = [...]` 已经能支撑：

- 共享 foundation
- project 私有 `lib/`
- 多文件 `src/`
- `axc run` 与 `axc build` 对 project 目录的基本装载

这意味着“多文件一做大就立刻散掉”这个问题，第一轮已经被压住了。

## 当前阶段的真实目标

当前阶段不要再把自己写成“继续补最小可写工具内核”的启动期文档。

当前阶段真正该追求的是：

- 把现有五类 project-backed 样例固化成代表集
- 让这些样例进入更硬的 `check / run / build / smoke` 验证
- 继续减少这些样例里的重复 helper
- 让“下一步该补什么”由现有样例暴露，而不是靠继续发明第六第七类样例

## 当前定义下，什么算“内核已到位”

满足下面这些条件，才算“最小可写工具内核”第一版基本成立：

- 已有一组真实工具风格样例可以自然 `check / run`
- 这些样例覆盖至少三类任务：
  - CLI 参数与输出整理
  - 文本处理与报告生成
  - 工作区 / 目录 / 文件辅助任务
- AX 侧共享 foundation 已开始替代示例里的重复 helper
- `AX.toml + sources = [...]` 没有明显成为当前样例扩张的主阻塞
- 新增 project 样例时，不需要每次都大面积补新的 builtin
- 现有样例已经足以暴露“下一步更值的是最小 collections，还是 import/module”

更直白地说：

- 到这个点，AX 还不是成熟语言
- 但它已经不再只是“能跑玩具样例”的原型
- 当前更像“第一版最小可写工具内核已接近成形，下一步该转入固化和验收”

## 当前最值得做的东西

### 1. 固化现有五类 project-backed 样例资产

当前最值钱的不是继续增加样例名字，而是把已经存在的五类样例收成一组代表资产。

当前更适合作为代表集核心候选的是：

- [`examples/project_directory_index/`](./examples/project_directory_index/)
- [`examples/project_text_normalize/`](./examples/project_text_normalize/)
- [`examples/project_release_promote/`](./examples/project_release_promote/)

它们更适合作为下一轮 `run / build / smoke` 的稳定代表，因为：

- 平台依赖更少
- 输入输出更可控
- 更接近真实文件工具工作流

[`examples/project_command_capture/`](./examples/project_command_capture/) 和 [`examples/project_command_batch/`](./examples/project_command_batch/) 仍然很重要，但更适合作为“命令执行能力验证样例”，而不是最先扩入所有 build/smoke 代表集的核心样例。

### 2. 把现有样例接进更硬的验证链路

下一步最值钱的不是继续写第六类 project 样例，而是：

- 让现有代表样例有更硬的 `check / run / build` 验证
- 让它们进入更系统的 smoke 或后续 benchmark 代表集
- 让这些样例真正承担“后续 backend / 组织能力 / 标准库补什么”的判断任务

### 3. 用现有样例逼出下一个真实缺口

当前最值得被真实样例继续检验的问题，不是 `match` 或泛型。

当前更值得被样例逼出来的，是：

- 最小 collections 能力
- `import / module` 是否真的已经到必须推进的时候

补充判断：

- 2026-04-25 最小 collections 第一批已先以 `string_list` 落地，证明代表样例里的“动态字符串条目聚合”确实值得被语言原生承接
- 2026-04-25 已在 project 样例里验证递归目录遍历与递归搜索，说明“更深目录遍历”当前更像样例层 / 共享库层继续推进的能力，而不是下一编译器主缺口
- 所以下一步不再是抽象讨论“要不要有最小 collections”，而是继续观察 `string_list` 是否已经撑住当前样例，还是还要补第二批 collections

处理原则很简单：

- 先让现有五类样例继续往真实任务靠
- 再看它们到底卡在哪
- 再决定是补语法、补库，还是继续沿现有 project 组织方式推进

### 4. 精炼 foundation 第一版，而不是为完整感继续加壳

foundation 的下一步重点不该是“看起来像标准库越来越大”。

下一步更该做的是：

- 继续收敛重复 helper
- 让已有模块边界更稳定
- 只在两个以上真实样例已经重复时，才继续往 foundation 收

标准很简单：

- 如果一个 helper 还没有在两个以上真实样例里稳定重复，就先不要为了“像标准库”硬收进去

## 当前不应该优先补的东西

下面这些东西不是永远不做，但当前不应该压过“固化现有内核基线”这条主线：

- `match`
- 泛型
- 宏
- `async / await`
- 异常系统
- 复杂包系统
- 通用 FFI
- `AX import -> Cargo crate` 桥接
- 官方 `network / concurrency` 标准库承诺
- 围绕某一代模型 tokenizer 的表面优化
- 在现有五类样例还没固化前继续无节制新增 project 样例品类

原因很简单：

- 它们表面积大
- 它们会快速放大语义、解释器、diagnostics、文档和测试负担
- 它们很容易把项目从“证明价值”带偏成“先堆语言面”

## 当前协作边界

这条线当前默认建议负责的文件：

- [`foundation/`](./foundation/)
- [`examples/`](./examples/)
- [`tests/interface_snapshots.rs`](./tests/interface_snapshots.rs)
- [`README.md`](./README.md)
- [`docs/README.md`](./docs/README.md)
- [`docs/host-runtime-boundary.md`](./docs/host-runtime-boundary.md)

这条线可以按需进入，但不应默认主动扩面的文件：

- [`src/semantic/checker/builtin.rs`](./src/semantic/checker/builtin.rs)
- [`src/interpreter.rs`](./src/interpreter.rs)
- [`src/project.rs`](./src/project.rs)
- [`src/build.rs`](./src/build.rs)

进入这些文件的条件应该是：

- 现有代表样例真的被卡住
- 不改这里就无法继续验证样例
- 改动能够直接换回更真实的样例或更硬的验证链路

这一线当前不建议主动改：

- [`src/ai.rs`](./src/ai.rs)
- [`benchmarks/repair-cases.json`](./benchmarks/repair-cases.json)
- [`benchmarks/repair-cases-smoke.json`](./benchmarks/repair-cases-smoke.json)
- [`benchmarks/repair-candidates/`](./benchmarks/repair-candidates/)
- [`scripts/export-repair-benchmark.ps1`](./scripts/export-repair-benchmark.ps1)
- [`scripts/run-repair-benchmark.ps1`](./scripts/run-repair-benchmark.ps1)
- [`scripts/score-repair-benchmark.ps1`](./scripts/score-repair-benchmark.ps1)
- [`scripts/compare-repair-feedback.ps1`](./scripts/compare-repair-feedback.ps1)
- [`scripts/compare-repair-modes.ps1`](./scripts/compare-repair-modes.ps1)
- [`scripts/smoke-compare-repair-feedback.ps1`](./scripts/smoke-compare-repair-feedback.ps1)
- [`scripts/smoke-compare-repair-modes.ps1`](./scripts/smoke-compare-repair-modes.ps1)
- [`scripts/smoke-repair-benchmark.ps1`](./scripts/smoke-repair-benchmark.ps1)
- [`docs/repair-benchmark.md`](./docs/repair-benchmark.md)
- [`docs/repair-adapter-spec.md`](./docs/repair-adapter-spec.md)
- [`docs/diagnostics-schema.md`](./docs/diagnostics-schema.md)

## 当前高冲突区

下面这些文件如果多人同时改，很容易冲突或口径漂移：

- [`README.md`](./README.md)
- [`PLAN.md`](./PLAN.md)
- [`WORKLIST.md`](./WORKLIST.md)
- [`规划.md`](./规划.md)
- [`tests/interface_snapshots.rs`](./tests/interface_snapshots.rs)

处理原则：

- 不在每个小提交里都顺手改这些文件
- 只有当一个里程碑真正收口时，再集中同步
- 如果只是推进单个样例或单个 foundation helper，优先把变更收在实现和局部测试里

## 这条线的默认交付顺序

当前阶段新增一项能力时，默认按下面顺序落地：

1. 先选一个现有代表样例或样例族
2. 如果样例真的被卡住，再补最小必要的 checker / interpreter / project / build 实现
3. 再决定是否需要抽成共享 foundation 或 project 私有 helper
4. 再补 `run / build / smoke / interface snapshots` 这一层验证
5. 最后再同步用户可见文档与施工清单

这个顺序的原因很简单：

- 先让问题由真实样例暴露
- 再做最小必要实现
- 再让能力稳定复用
- 最后才把文档和更外围资产跟上

## 这条线当前推荐的施工批次

### 第一批：固化五类样例代表集

优先目标：

- 不再优先继续增加样例品类
- 先从现有五类 project-backed 样例里挑出代表集
- 让代表集承担下一步判断任务

这一批的成功标志：

- 可以明确说出哪几个样例是当前最核心的 `run / build / smoke` 代表

### 第二批：验证现有 project 组织方式还能撑多久

优先目标：

- 继续沿 `AX.toml + sources = [...]` 路线推进
- 看现有代表样例是否已经持续暴露 project 组织痛点

这一批的成功标志：

- 可以明确回答：
  - 现有 project 组织是否还够继续撑下一轮工具样例
  - `import / module` 是否已经比 `minimal collections` 更值

### 第三批：用代表样例逼出下一批能力

优先目标：

- 先让现有样例继续更接近真实工作流
- 再判断下一批最值钱的是：
  - 最小 collections
  - `import / module`

这一批的成功标志：

- 下一批能力排序不再靠感觉，而是能明确指出是哪几个样例暴露了哪个缺口

## 暂不拍板的开放问题

下面这些问题可以讨论，但当前不应提前拍板：

- `import / module` 到底是近期必须项，还是 `AX.toml` 还能继续撑一段
- `continue` 是否真的比补库更值
- 最小 `collections` 到底要做到哪
- 什么时候从“共享 foundation”升级成真正的官方标准库层
- 当前五类 project-backed 样例里，哪些应该先进入 build/smoke 代表集

处理原则：

- 先让现有代表样例把问题暴露出来
- 再决定是补语法、补库，还是继续沿现有组织方式推进

## 变更前自检清单

准备改这条线时，先过一遍下面这几条：

1. 这次改动是在固化现有内核，还是在重新打开启动期范围？
2. 没有这项能力，现有代表样例是否真的写不顺？
3. 这次改动是否会顺手踩到 A 线文件？
4. 这次改动是否需要同步全局文档，还是可以先收在局部实现和测试里？
5. 这项能力落地后，能不能立刻让现有样例更稳定，或者更明确暴露下一个缺口？

只要上面五条里有两条答不上来，就先不要动大面。

## 当前一句话结论

当前这条线的正确目标不是：

- “继续补一轮最小可写工具内核”

而是：

- “先证明现有第一版最小可写工具内核已经成立，再让现有五类样例逼出下一个最值钱的缺口”

更直接一点：

- 现在先验内核、固内核
- 不是继续按启动期逻辑无限补面
