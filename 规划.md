# AX 规划

## 文档定位

- [`PLAN.md`](C:/Users/xiaoy/Desktop/A语言/AX/PLAN.md) 管方向、原则、边界和继续条件。
- [`WORKLIST.md`](C:/Users/xiaoy/Desktop/A语言/AX/WORKLIST.md) 管当前施工项、状态和完成记录。
- 本文件管长期执行顺序、阶段切换条件，以及“现在最该做什么”。

一句话分工：

- `PLAN.md` 管“为什么做”
- `WORKLIST.md` 管“最近在做什么”
- `规划.md` 管“从现在到后面应该按什么顺序做”

状态说明：

- `[x]` 已完成
- `[~]` 进行中
- `[ ]` 待做

## 无时间版关卡规则

- 本文件默认采用“关卡验收”，不采用“某周必须完成某阶段”的时间驱动写法。
- 每个阶段至少说明四件事：
  - `目标`
  - `必须产物`
  - `通过阈值`
  - `未通过时怎么办`
- 任何阶段未达标，不进入下一阶段；允许继续打磨、回滚方案或缩小范围。
- `PLAN.md` 管方向和原则，`WORKLIST.md` 管具体施工，本文件重点管阶段切换条件和执行顺序。

## 当前判断

AX 的长期终点当然可以包括自举，但那是后期关卡，不该反过来吞掉当前主线。

当前更准确的表述是：

- 现在的编译器仍然明确是 `Rust` 种子实现。
- 现在要证明的不是“AX 能不能马上自举”，而是“AX 作为一门 AI-first 语言，是否真的在生成、诊断、修复和工具化上有优势”。
- 自举应当建立在前面几层已经站稳之后：
  - 语言核心稳定
  - 诊断与 AI 反馈协议稳定
  - benchmark 有证据
  - AX 已经能承载一批真实工具风格程序

所以，之前把规划写成“围绕自举重排一切”是不合适的。  
更合理的长期路线应该是：

`Rust 种子编译器做实 -> AI 反馈协议与 benchmark 做实 -> 按证据扩语言与工具面 -> 原生后端成熟 -> AX 承载真实工具 -> 再进入自举准备`

## 我们现在到底在做什么

AX 当前不是“玩具解析器项目”，但也还没到“语言价值已被证明”的阶段。

它现在最像的是：

> 一个已经有前中端骨架、解释执行链路、结构化诊断和 AI 修复协议雏形的语言原型项目。

当前已经有的基础：

- `axc check / run / ast / hir / mir / fmt / build`
- `Lexer -> Parser -> AST -> HIR -> Semantic -> Interpreter`
- 结构化 diagnostics 与 `--json --ai`
- repair benchmark、comparison、smoke 与 CI
- 项目 manifest、快照测试、示例与文档

所以接下来不该把精力主要放在“继续内部拆层”或“提前喊自举”，而应该放在：

1. 固定外部接口和可回归资产
2. 固定 benchmark 和性能基线
3. 提高 AI 反馈协议的质量与稳定性
4. 用真实工具风格样例倒逼下一批语言能力
5. 在证据支持下扩语言与后端

## 当前项目状态

AX 现在已经不是空壳了，而是一个有前端、语义、解释执行、AI 反馈原型、benchmark 骨架和构建骨架的语言原型。

### 已经完成的主干

- 已有 `axc check / run / ast / hir / mir / fmt / build` 命令入口。
- 已有 `Lexer -> Parser -> AST -> HIR -> Semantic -> Interpreter` 主链路。
- 已有结构化 diagnostics，且支持 AI 增强反馈。
- 已有 repair benchmark 数据集、adapter、对比脚本和 smoke 流程。
- 已有固定长度数组、数组字面量、索引读取、数组元素赋值的前中端与解释器贯通。
- 已有最小项目形态：`AX.toml`、单包、单入口工程。

### 当前还没补齐的关键缺口

- 外部契约还需要继续稳定：
  - diagnostics schema
  - AI feedback schema
  - AST/HIR/MIR/build 输出形状
  - 文档与实现的一致性
- AI 修复闭环还不够硬：
  - 规则触发仍需更稳定
  - 会话状态还要继续版本化和约束
  - 高优先级错误还要继续补规则覆盖测试
  - 运行期错误还需要逐步纳入结构化反馈
- 语言面已经过了第一轮“原型可用”关口：
  - 切片、字符串拼接、统一 `len(value)`、`to_string(value)`、嵌套可写路径和 `break;` 都已落地
  - `match`、真实 import/module、`continue` 和更高层遍历语法仍未进入当前执行面
  - 当前主瓶颈已经不再是“还差哪一条小语法”，而是“最小工具宿主能力和真实样例是否足够支撑下一轮验证”
- benchmark 代表性还不够：
  - smoke 证据链已经稳住，但 full repair benchmark 与 compare replay 资产还偏薄
  - 现有 benchmark 还不足以覆盖“真实工具代码里的常见误用”这一层
- 原生编译还没真正落地：
  - `axc build` 现在还是构建骨架，不是最终原生可执行产物
- 产品化还不够：
  - 最小标准库面还薄
  - benchmark 还没形成固定节奏报告
  - 对外“如何让 AI 正确写 AX”的资产还需要继续固化

## 总路线

AX 的长期路线按下面顺序执行：

1. `[~]` 阶段 1：原型收口与接口稳定
2. `[~]` 阶段 2：AI 修复协议硬化与 benchmark 常态化
3. `[ ]` 阶段 3：固化最小可写工具内核与代表样例
4. `[ ]` 阶段 4：原生后端与 `axc build` 从骨架走向可用
5. `[ ]` 阶段 5：产品化交付能力
6. `[ ]` 阶段 6：自举准备与局部自举验证

这 6 个阶段里，自举是后段目标，不是当前组织原则。

## [~] 阶段 1：原型收口与接口稳定

### 目标

让当前仓库已经存在的能力不打架，形成可靠的“种子编译器基线”。

### 必须产物

- 稳定的 `check/json/ai` 输出契约
- 稳定的 AST/HIR/MIR/build 快照
- 文档与实现一致性清单
- 明确的“已支持 / 未支持 / guidance-only”边界

### 本阶段优先级顺序

#### P0：先稳外部契约

- 稳定 `check --json` 与 `check --json --ai` 的输出形状。
- 稳定 AST/HIR/MIR/build 的接口快照。
- 继续同步 [`SYNTAX.md`](C:/Users/xiaoy/Desktop/A语言/AX/SYNTAX.md)、[`README.md`](C:/Users/xiaoy/Desktop/A语言/AX/README.md)、[`PLAN.md`](C:/Users/xiaoy/Desktop/A语言/AX/PLAN.md) 与真实实现。
- 明确哪些语法是“已支持”，哪些是“只给 guidance，不可执行”。

#### P0：再稳 AI 修复协议

- 继续收敛 `src/ai.rs` 的规则触发方式，尽量少依赖脆弱文案匹配。
- 2026-04-25 已把模块误用诊断与首批高价值类型错误接入稳定 `DiagnosticKind`，`src/ai.rs` 开始优先消费内部语义标签；这说明 AI 协议硬化已经从 `message.contains(...)` 触发，往更可重构的内部 contract 推进了一步。
- 2026-04-25 又继续把 parser 侧高频 `P0001` 诊断也接入稳定 `DiagnosticKind`，当前覆盖缺分号、缺右括号、缺右花括号与顶层声明错误；这让最常见的语法修复规则也开始摆脱对文案内容的强绑定。
- 2026-04-25 进一步把 parser 的详细 note/suggestion 生成逻辑也改成优先看 `DiagnosticKind`，说明“更详细、接近 Rust 风格的编译器反馈”这件事，现在也开始建立在稳定内部标签上，而不是脆弱文案上。
- 给 AI session state 做版本字段和兼容策略。
- 补“高优先级错误码 -> 稳定 rule_id”覆盖测试。
- 把教学层升级逻辑固定住，确保同源码 + 同 session = 同输出。
- 默认快路径保持轻量，不能因为 AI 教学层把基础 `check` 跑慢一大截。

#### P1：补当前原型最影响实践的缺口

- 基于已落地的切片、字符串和空数组零长度策略，继续补下一批最影响工具代码表达密度的能力。
- 在切片已落地后，继续用真实工具样例验证它是否足够支撑下一批遍历能力。
- 审查当前 examples、坏例子、benchmark cases 有没有和真实实现脱节。
- 继续补齐语义、解释器、AI guidance 三者对新特性的同步。

#### P1：补运行期结构化反馈

- 逐步把 `axc run` 的失败也纳入结构化 diagnostics。
- 让 runtime error 也能进入 AI 修复链，而不是只靠文本输出。

### 通过阈值

- `axc check --json` 与 `axc check --json --ai` 字段形状固定
- 相同输入 + 相同 session state 下，AI 增强输出确定性一致
- `README`、[`SYNTAX.md`](C:/Users/xiaoy/Desktop/A语言/AX/SYNTAX.md)、[`PLAN.md`](C:/Users/xiaoy/Desktop/A语言/AX/PLAN.md) 与实现没有明显冲突项
- 基础 `check` 性能无明显回退

### 当前优先事项

- 固定 benchmark 的输入输出协议
- 固定最小性能基线
- 清理文档与实现的分叉
- 继续做高频错误的 AI 规则卡与测试

### 未通过时怎么办

- 不进入阶段 2，继续修接口漂移、文档冲突、确定性问题和性能回退。

## [~] 阶段 2：AI 修复协议硬化与 benchmark 常态化

### 目标

把 AX 最有辨识度的东西从“想法不错”变成“可回归、可比较、可集成”。

### 必须产物

- 规则注册表
- session 版本策略
- 高频错误码到 `rule_id` 的覆盖测试
- 稳定 benchmark 报告格式
- 三模式比较闭环：
  - 冷启动
  - 基础 diagnostics
  - AI 增强 diagnostics

### 通过阈值

- 高频错误码有稳定 `rule_id` 映射
- session 文件有 `version` 校验与兼容策略
- `--json` 与 `--json --ai` 职责边界清晰
- benchmark 报告可重复生成并可比较 repair lift

### 当前优先事项

- 把 benchmark 从“能跑”变成“固定协议”
- 继续补高价值错误的 AI 增强上下文
- 给性能与反馈质量建立可持续观测基线

### 未通过时怎么办

- 不进入阶段 3，继续补规则注册、session 版本策略、错误码覆盖测试和报告固定性。

## [ ] 阶段 3：固化最小可写工具内核与代表样例

### 目标

不继续按启动期思路无上限补新能力，而是先把现有第一版共享 foundation 和五类 project-backed 真实工具样例固化成“可验收的代表集”，再让这些代表样例决定下一批最值钱的缺口。

### 必须产物

- 一组明确的代表样例分层：
  - 核心代表集
  - 宿主能力验证集
- 现有代表样例更硬的 `check / run / build / smoke` 验证
- 共享 foundation 的第一版稳定边界
- 一份由代表样例驱动的“下一缺口排序”结论：
  - 更深目录遍历
  - 最小 collections
  - `import / module`

### 通过阈值

- 至少三类核心代表样例可稳定 `check / run`，且进入更硬的 `build / smoke` 验证目标
- 当前五类 project-backed 样例不再频繁因为缺少新的 builtin 而被迫扩面
- 共享 foundation 已经开始稳定承接重复 helper，而不是每个 project 各自长一套私有壳
- 能明确回答：下一步更值钱的是更深目录遍历、最小 collections，还是 `import / module`

### 预期优先方向

- 先固化现有五类 project-backed 样例，而不是继续发明第六、第七类样例
- 先把 [`examples/project_directory_index/`](C:/Users/xiaoy/Desktop/A语言/AX/examples/project_directory_index/)、[`examples/project_text_normalize/`](C:/Users/xiaoy/Desktop/A语言/AX/examples/project_text_normalize/) 和 [`examples/project_release_promote/`](C:/Users/xiaoy/Desktop/A语言/AX/examples/project_release_promote/) 收成核心代表集
- 把 [`examples/project_command_capture/`](C:/Users/xiaoy/Desktop/A语言/AX/examples/project_command_capture/) 和 [`examples/project_command_batch/`](C:/Users/xiaoy/Desktop/A语言/AX/examples/project_command_batch/) 作为宿主能力验证集，重点压 `process / env / path / fs`
- 继续把重复 project helper 收敛进共享 AX 基础层；当前 repo 根目录 [`foundation/`](C:/Users/xiaoy/Desktop/A语言/AX/foundation/) 已沉淀 `cli / report / text / search / file_kind / workspace`，并完成了从 `examples/foundation/` 的迁移
- 在代表样例与 foundation 边界都稳住之前，不按想象继续扩更大语法面

### 未通过时怎么办

- 不继续扩下一批语法，也不急着深挖 backend；先补代表样例、foundation 边界和下一缺口排序。

## [ ] 阶段 4：原生后端与 `axc build`

### 目标

让 `axc build` 从“后端骨架”走向“真实可交付产物”。

### 必须产物

- 更稳定的 MIR 语义
- 原生后端接入
- 构建失败的结构化诊断
- 一组真实样例的 native build 结果

### 通过阈值

- 一组真实工具样例可原生编译并运行
- 构建失败不只是黑盒文本
- 编译耗时与资源开销进入可接受范围

### 约束

- 后端推进不能压过 benchmark 和语言主线
- 没有证据前，不把 `build` 包装成“后端已经成熟”

### 未通过时怎么办

- 不进入产品化交付，继续收敛 MIR 语义、产物契约和构建诊断。

### 对应的 `WORKLIST` 进入标准

- 只有当语言前中端、diagnostics、repair benchmark 和 `build` 骨架都已经基本稳定后，才正式启动 [`WORKLIST.md`](C:/Users/xiaoy/Desktop/A语言/AX/WORKLIST.md) 里的 `P2-2`。
- 启动后必须把后端工作限定在“真实 native 产物 + 结构化构建失败诊断”两个核心目标上，不扩散成新的并行主线。

## [ ] 阶段 5：产品化交付能力

### 目标

让外部开发者不靠口头指导，也能把 AX 用起来。

### 必须产物

- 最小标准库面
- 稳定安装与使用流程
- 对外接口文档
- 更像产品而不是实验仓库的文档入口

### 最小标准库面优先范围

- `io`
- `fs`
- `path`
- `env`
- `process`
- `string`

### 通过阈值

- 新用户可按文档完成：
  - 安装
  - `check / run / build`
  - 一次 repair benchmark
- 一个发布周期内，对外 schema 基本稳定

### 未通过时怎么办

- 不进入自举准备，继续补安装流程、标准库面和对外接口文档。

### 对应的 `WORKLIST` 进入标准

- 只有当样例和 benchmark 已明确显示标准库缺口比继续补语法更阻塞时，才正式启动 [`WORKLIST.md`](C:/Users/xiaoy/Desktop/A语言/AX/WORKLIST.md) 里的 `P2-3`。
- 标准库扩展必须围绕 `io / fs / path / env / process / string` 这类最小工具能力推进，不做大而全生态叙事。

## [ ] 阶段 6：自举准备与局部自举验证

### 目标

在前面几层都已经站稳之后，再进入 AX 写 AX 编译器子集的阶段。

### 必须产物

- AX 写成的部分前端或工具链子模块
- 双实现交叉验证流程
- 自举前的能力缺口清单

### 通过阈值

- AX 能稳定承载一部分编译器风格逻辑
- 双实现对照结果可重复
- benchmark、工具链与已有能力没有明显回退

### 说明

这里的自举是长期目标，不是当前宣传口径，也不是当前主线 KPI。

### 推荐顺序

1. 先用 AX 重写 lexer / parser 的子集。
2. 再做语义模块的局部替换。
3. 再做交叉校验与双实现比对。
4. 最后尝试自编译与稳定复现。

### 未通过时怎么办

- 不把“自举”当主线宣传，继续维持 Rust 种子实现并补稳定性。

### 对应的 `WORKLIST` 进入标准

- 只有当 Rust 种子编译器稳定、AX 已经能写出一批真实工具风格程序后，才正式启动 [`WORKLIST.md`](C:/Users/xiaoy/Desktop/A语言/AX/WORKLIST.md) 里的 `P2-4`。
- 自举只允许按“局部子模块 -> 双实现对照 -> 扩范围”的顺序推进，不能反过来吞掉当前主线。

## 当前最现实的执行顺序

如果只看“现在下一步应该做什么”，顺序是：

1. `[x]` 固定 benchmark 与性能基线
2. `[x]` 固定 `check/json/ai` 接口与回归资产
3. `[~]` 扩 full repair benchmark 资产与 compare replay 基线
4. `[~]` 继续补高价值 AI 规则、会话策略和运行期 repair 证据
5. `[~]` 固化现有五类 project-backed 样例为代表集，并接进更硬的 `check / run / build / smoke`
6. `[x]` 先冻结 `import / module` 的最小方案、命名边界与 `AX.toml + sources` 兼容策略
7. `[~]` 进入 `import / module` 的 parser / resolver / diagnostics 第一刀实现
8. `[ ]` 再根据新方案决定是否继续补第二批 `minimal collections`、更大语法面或 `axc build`

## 当前 P0 / P1 / P2

### [~] P0：现在必须先做

- `[x]` benchmark 三模式固定报告
- `[x]` `check/json/ai` schema 稳定
- `[x]` AI 规则覆盖与 session 版本策略
- `[x]` 最小性能基线
- `[x]` full repair benchmark 资产与 compare replay 基线扩容
- `[~]` AI 规则触发、运行期 repair 合同与文档事实的第二轮收口
- `[x]` 工具风格样例资产（已落地 token 扫描、状态机与 block 总结样例）
- `[x]` `run` 结构化错误输出

### [ ] P1：P0 稳住后马上做

- `[x]` 首轮语言表达密度补齐：切片、空数组零长度策略、字符串拼接、`len(value)`、`to_string(value)`、嵌套可写路径、`break;`
- `[~]` 最小工具宿主能力第一批：`process / env / path / fs / string`
  当前五类 project-backed 样例已经把第一轮能力压出来了，下一步重点是把接口边界、误用反馈和共享基础层收稳，而不是继续无上限铺面。
  2026-04-25 已先补上首批宿主运行期 AI guidance，把“缺可读文件/目录”和“process 启动失败或 capture 非零退出”收成稳定 `rule_id`，说明这条线已经开始从“能用”走向“可回归”。
  2026-04-25 还把缺可读文件的 runtime case 接进 full repair benchmark，并补了共享修复候选；这说明 P1-10 已经开始从宿主能力扩面进入证据链收口。
  2026-04-25 又把 `process_capture` 非零退出 case 接进 full repair benchmark，说明宿主 runtime 的 repair 证据链已经不再只覆盖算术/数组，而开始覆盖真实工具型 host 误用。
  2026-04-25 再把 `fs_read_dir` 缺可读目录 case 接进 full repair benchmark，说明宿主 runtime repair 已经开始形成“小而稳定”的 host 误用回归面，而不是零散单点规则。
  2026-04-25 已修复 Windows 下 `env_has/env_get` 的环境变量大小写兼容问题，`project_command_batch` 这类宿主能力样例重新回到可回归状态；这说明当前这条线真正的重点是把 host boundary 收紧，而不是急着继续扩 builtin 面。
  2026-04-25 又补强了 Windows 下 benchmark / interface snapshot 的 PowerShell 链路：临时 runner 脚本改为带 BOM 落盘，benchmark 导出/打分也改成用临时 `axc` 副本执行，中文路径与 exe 锁不再轻易把回归结果打成假红。
- `[x]` 固化现有五类 project-backed 样例为代表集，并接进更硬的 `check / run / build / smoke`
  当前已经形成核心代表集与宿主能力验证集，并补上了代表项目 build smoke。
- `[x]` 用代表样例与 benchmark 结果重新判断：递归目录遍历已在 project 样例里验证可行，说明它更像样例/共享库层可推进能力，而不是下一编译器主缺口
- `[x]` 下一批更值钱的核心缺口：最小 collections 第一批
  当前已先以 `string_list` 落地第一版，证明代表样例不再只能靠纯流式字符串累积表达动态条目聚合；下一步重点已经从“要不要有最小 collections”转为“`import / module` 是否已经升级成新的主阻塞”。
- `[x]` `import / module` 进入条件复查
  2026-04-25 复查结论：共享 [`foundation/`](C:/Users/xiaoy/Desktop/A语言/AX/foundation/) 已沉淀 `cli / report / text / search / file_kind / workspace` 六块 helper，七个 project-backed 工程稳定采用 `sources = ["../../foundation", "lib"]`，代表项目也进入 3-4 个 `.ax` 文件、约 88-122 行的量级；重复 helper 名称开始对扁平全局命名空间形成真实压力，这说明 `import / module` 已经从“观察项”升级成第一批 `minimal collections` 之后的下一主阻塞。
- `[x]` `import / module` 最小方案与迁移边界
  2026-04-25 已完成第一版设计冻结，明确了：文件发现继续走 `AX.toml + sources`，支持文件一文件一模块，入口文件显式 `import`，跨模块名称默认走全限定路径，当前不引入 alias / wildcard / pub / 包管理；详见 [`docs/import-module-minimal-design.md`](C:/Users/xiaoy/Desktop/A语言/AX/docs/import-module-minimal-design.md)。
- `[~]` `import / module` parser / resolver / diagnostics 第一刀
  2026-04-25 已完成第一段落地：`module` / `import` 已进入 lexer + parser，编译前端现会保留每个 source unit 的 header 元信息；`Project` 已能从 `AX.toml + sources` 推导 support source 的期望模块路径并拦截重复模块根别名；语义侧已补上 support source 缺少 `module`、模块路径错配、重复模块、重复 `import`、导入不存在模块、跨模块引用缺少 `import` 等首批 diagnostics，并加入 [`examples/project_module_smoke/`](C:/Users/xiaoy/Desktop/A语言/AX/examples/project_module_smoke/) 做 `axc check` 验证。
  同日又已补上 qualified function/type/enum 的 HIR lowering 兼容，最小模块样例现已能稳定通过 `axc hir` 并执行 `axc run`。
  同日也已把 AI 反馈层同步到当前模块语义：旧的 “import/module 尚未支持” 文案已退场，现改为稳定覆盖 `S0037`-`S0043` 的模块规则卡，并补了 project 级 AI 回归测试。
  同日也已把 repair benchmark 的旧 import/module 占位 case 切到当前真实语义，并更新设计文档状态说明；当前模块线剩余收尾重点收敛为：补更完整的接口回归，并明确 `build`/后续扩展边界。

### [ ] P2：有证据再推进

- `[ ]` 更大语法扩展
  - 进入条件：新的真实工具样例已经明确指出某个语法缺口比继续补标准库或 backend 更值。
- `[ ]` 原生后端深化
  - 进入条件：`build` 骨架和前中端契约已基本稳定，且已经有一批值得拿来做原生验证的真实工具样例。
- `[ ]` 更完整标准库
  - 进入条件：最小宿主能力第一批已落地，但真实工具样例仍被标准库面阻塞。
- `[ ]` AX 局部重写前端子集
  - 进入条件：Rust 种子实现稳定，AX 已具备承载部分编译器逻辑的语言和工具能力。

## 现在不要做的事

当前阶段不该把精力主要投到下面这些方向：

- 继续无限细拆 Rust `semantic`
- 围绕“自举”重写全部路线文档
- 在没有 benchmark 证据前大扩语法面
- GUI / Web / async / 宏系统 / 复杂泛型
- 把 `build` 提前包装成成熟编译产线
- 为了“看起来厉害”而提前铺太多外围系统

这些方向以后不是不能做，只是现在做，收益不如把主线做实。

## 统一验收模板

以后新的阶段任务、子任务或 [`WORKLIST.md`](C:/Users/xiaoy/Desktop/A语言/AX/WORKLIST.md) 条目，尽量按这个模板写：

- `目标`：这次改动要解决什么
- `输入`：依赖哪些样例、脚本、接口或基线
- `输出`：新增或变更的文件、接口、产物或诊断
- `通过条件`：必须满足的可判定条款
- `回归保障`：对应测试、快照、benchmark 或 smoke
- `不做范围`：这次明确不解决什么

## 维护规则

- 每完成一部分实际工作，都要检查本文件是否需要同步更新。
- 只要当前主线、阶段顺序、P0/P1/P2 判断发生变化，就必须同步更新本文件。
- 若只是实现推进但不改路线，也至少要确认本文件仍然准确。

## 当前一句话结论

AX 现在最该做的，不是把全部路线都改写成“为了尽快自举”。

AX 现在最该做的是：先把 `Rust` 种子编译器、AI 反馈协议和 benchmark 证据链做实，再补最小工具宿主能力与真实工具样例，最后再让 backend 和长期自举建立在这些事实之上。
