# AX 规划

## 文档定位

- [`PLAN.md`](C:/Users/xiaoy/Desktop/A语言/AX/PLAN.md) 是唯一主计划，负责回答“为什么做、做到什么算成功、哪些原则不能破”。
- [`WORKLIST.md`](C:/Users/xiaoy/Desktop/A语言/AX/WORKLIST.md) 是当前施工清单，负责回答“最近这批具体任务做没做完”。
- 本文件是**执行路线图**，负责回答“从现在到后面该按什么顺序推进、每一步先补什么、哪些东西必须等前面完成才能做”。

一句话说清三者分工：

- `PLAN.md` 管方向和关卡。
- `WORKLIST.md` 管当天施工状态。
- `规划.md` 管从头到尾的推进顺序。

## 当前项目状态

AX 现在已经不是空壳了，而是一个**有前端、语义、解释执行、AI 反馈原型、benchmark 骨架、构建骨架**的语言原型。

### 已经完成的主干

- 已有 `axc check / run / ast / hir / mir / fmt / build` 命令入口。
- 已有 `Lexer -> Parser -> AST -> HIR -> 语义检查 -> 解释器` 主链路。
- 已有结构化 diagnostics，且支持 AI 增强反馈。
- 已有 repair benchmark 数据集、adapter、对比脚本和 smoke 流程。
- 已有固定长度数组、数组字面量、索引读取、数组元素赋值的前中端与解释器贯通。
- 已有最小项目形态：`AX.toml`、单包、单入口工程。

### 当前还没补齐的关键缺口

- **外部契约还需要继续稳定**：
  - diagnostics schema
  - AI feedback schema
  - AST/HIR/MIR/build 输出形状
  - 文档与实现的一致性
- **AI 修复闭环还不够硬**：
  - 规则触发仍需更稳定
  - 会话状态还要继续版本化和约束
  - 高优先级错误还要继续补规则覆盖测试
  - 运行期错误也需要逐步纳入结构化反馈
- **语言面还处于“原型可用但未收口”状态**：
  - `match` 还没做
  - 切片还没做
  - 空数组字面量推导还没定
  - import/module 还是 guidance，不是真正实现
- **原生编译还没开始真正落地**：
  - `axc build` 现在还是构建骨架，不是最终原生可执行产物
- **产品化还不够**：
  - 最小标准库面还薄
  - benchmark 还没形成持续周报式闭环
  - 对外“如何让 AI 正确写 AX”的资产还需要继续固化

## 总体推进原则

后面的工作顺序不应该是“哪里想做就做哪里”，而应该固定成下面这条主线：

1. 先稳住**当前已经有的东西**，避免前端、语义、执行、文档、AI 反馈互相打架。
2. 再把**AI 修复协议**打磨成可回归、可比较、可 benchmark 的硬接口。
3. 再用 benchmark 验证 AX 的核心主张到底有没有成立。
4. 只有在 benchmark 给正信号之后，才继续扩语法面和原生后端。
5. 最后再谈更大交付面、自举和长期生态。

也就是说，AX 接下来的路线不是“拼命加特性”，而是：

**一致性 -> AI 闭环 -> benchmark 证据 -> 原生后端 -> 产品化 -> 自举**

## 从头到尾的执行路线

## 阶段 0：基础原型已搭起来

### 状态

- 状态：`已完成`

### 这一阶段已经拿到的结果

- `axc` 已经不只是 parser demo，而是有实际命令链和样例程序的原型工具。
- 解释器已经能跑一批真实 AX 例子。
- AI diagnostics、benchmark、adapter、快照测试已经有第一版骨架。

### 这阶段的意义

这一步最重要的不是“功能很多”，而是证明 AX 已经有一条真正能往下迭代的主干线，不需要再回到“要不要从零搭前端”的状态。

## 阶段 1：原型收口期

### 状态

- 状态：`当前主线`
- 时间位置：`现在就该做`

### 核心目标

把当前原型从“能跑”提升到“接口稳定、行为一致、适合继续扩展”。

### 为什么这一步必须先做

如果现在直接猛加新语法或直接冲原生后端，会出现几个问题：

- 文档和实现越来越偏
- diagnostics 越改越不稳定
- AI 反馈协议没有固定下来，benchmark 数据会失真
- 后端建立在未收口的前端和语义上，后面返工更重

### 本阶段优先级顺序

#### P0：先稳外部契约

- 稳定 `check --json` 与 `check --json --ai` 的输出形状。
- 稳定 AST/HIR/MIR/build 的接口快照。
- 继续同步 [`SYNTAX.md`](C:/Users/xiaoy/Desktop/A语言/AX/SYNTAX.md)、[`README.md`](C:/Users/xiaoy/Desktop/A语言/AX/README.md)、[`PLAN.md`](C:/Users/xiaoy/Desktop/A语言/AX/PLAN.md) 与真实实现。
- 明确哪些语法是“已支持”，哪些是“只给 guidance，不可执行”。

#### P0：再稳 AI 修复协议

- 继续收敛 `src/ai.rs` 的规则触发方式，尽量少依赖脆弱文案匹配。
- 给 AI session state 做版本字段和兼容策略。
- 补“高优先级错误码 -> 稳定 rule_id”覆盖测试。
- 把教学层升级逻辑固定住，确保同源码 + 同 session = 同输出。
- 默认快路径保持轻量，不能因为 AI 教学层把基础 `check` 跑慢一大截。

#### P1：补当前原型最影响实践的缺口

- 明确空数组字面量是要支持推导，还是继续稳定报 unsupported。
- 推进切片是否进入当前原型。
- 审查当前 examples、坏例子、benchmark cases 有没有和真实实现脱节。
- 继续补齐语义、解释器、AI guidance 三者对新特性的同步。

#### P1：补运行期结构化反馈

- 逐步把 `axc run` 的失败也纳入结构化 diagnostics。
- 让 runtime error 也能进入 AI 修复链，而不是只靠文本输出。

### 本阶段完成标志

- 当前已支持语法在文档、语义、执行、AI 规则里完全对齐。
- `check/json/ai/ast/hir/mir/build` 输出足够稳定，能长期当 benchmark 接口。
- 高价值错误码有稳定的 AI 规则映射和快照测试。
- 基础模式下的速度没有明显回退，AI 增强层只在显式请求时增加额外成本。

## 阶段 2：AI 修复闭环强化

### 状态

- 状态：`阶段 1 之后立即进入`

### 核心目标

把 AX 的核心差异化能力从“有想法”变成“能反复验证”的系统资产。

### 这一阶段真正要交付的东西

- 一套稳定的 AI 反馈 schema。
- 一套高频错误规则卡注册表。
- 一套修复上下文切片能力。
- 一套会话级分层教学机制。
- 一套能让外层 agent/IDE 稳定消费的协议。

### 优先顺序

1. 先固定 schema，不要让字段到处漂。
2. 再固定规则卡注册方式，不要把教学文案散在各模块里。
3. 再固定上下文切片范围，只给最小必要信息。
4. 最后才扩规则覆盖面和教学深度。

### 这一阶段特别要注意

- 编译器里不要塞厂商定制 prompt。
- 不要让“AI 增强输出”反过来污染基础诊断层。
- 不能为了更详细就把 token 成本和速度成本做爆。
- 不能让 session 记忆改变编译结果，只能改变教学层级。

### 本阶段完成标志

- `--json` 和 `--json --ai` 的职责边界清楚。
- 常见错误的 `rule_id / repair_goal / rule_card / fixits` 稳定。
- 外层工具已经可以只靠 AX 的结构化输出做单轮修复实验。

## 阶段 3：benchmark 闭环常态化

### 状态

- 状态：`AX 是否继续扩张的关键关卡`

### 核心目标

不再凭感觉判断 AX 是否“更适合 AI”，而是按固定协议周周可复现地比较。

### 这一步要做的事

- 固定 repair benchmark 数据集。
- 固定模型、预算、回合限制、adapter 调用方式。
- 固定三种模式的对比：
  - 只给冷启动提示
  - 给基础 diagnostics
  - 给 AI 增强 diagnostics
- 固定评分与报告输出。

### 为什么它重要

如果这一步不做，后面所有“扩语法”“做后端”“讲生态”都可能是在错误方向上加速。

### 本阶段完成标志

- 能稳定生成对比报告。
- 能回答 AI 增强反馈相对基础反馈到底提升了多少。
- 能清楚看见 AX 的收益来自哪里：
  - 语法本身
  - diagnostics 质量
  - AI teaching layer

### 进入下一阶段的前提

- benchmark 至少持续给出正信号。
- 没有证据之前，不扩大语言面，不搞大生态叙事。

## 阶段 4：按证据扩语言面

### 状态

- 状态：`必须晚于 benchmark 正信号`

### 核心目标

不是“把大家熟悉的语言特性全补齐”，而是只扩那些能明显帮助 AX 在目标任务里更好写、更好修、更好跑的能力。

### 推荐扩展顺序

1. 先补最容易影响当前实践闭环的能力。
   - 切片
   - 空数组字面量策略
   - 少量必要类型表面
2. 再补会明显改善表达力、但实现复杂度还可控的能力。
   - 最小 `match`
   - 更清晰的数据结构表面
3. 最后再考虑更复杂的语言能力。
   - 复杂泛型
   - 模块系统扩张
   - 更高级控制流糖衣

### 这一阶段必须遵守的边界

- 每加一个特性，都要同步：
  - parser
  - 语义
  - 解释器或后端
  - formatter
  - diagnostics
  - AI 规则
  - 文档
  - benchmark/task 示例
- 如果某个新特性对 benchmark 没明显帮助，就不要急着做。

### 本阶段完成标志

- 当前 AX 子集已经足够覆盖主要 benchmark 任务。
- “已支持语法”和“未支持语法”边界继续保持清晰。

## 阶段 5：原生后端与真正的 `axc build`

### 状态

- 状态：`中期主线`

### 核心目标

把 AX 从“解释器原型”推进到“能生成原生可执行文件的语言原型”。

### 推荐顺序

1. 先稳定 MIR 语义和 lowering。
2. 再接 Cranelift 或目标后端。
3. 再让 `axc build` 产出真正可运行的 Windows 原生程序。
4. 再处理 debug/release、产物布局、构建错误信息。

### 这一阶段要特别守住的底线

- 原生后端不能破坏前端和 diagnostics 的稳定性。
- 编译速度和日常开发体验不能明显恶化。
- AI 增强模式是可选层，默认构建快路径要尽量轻。
- 性能优化必须服务“可预测”，不是追求炫技式复杂优化器。

### 本阶段完成标志

- `axc build` 能真正生成目标平台可执行文件。
- 至少一批真实小工具能原生编译并跑通。
- 编译耗时、冷启动、内存占用进入可接受区间。

## 阶段 6：产品化与对外交付

### 状态

- 状态：`后端可用之后`

### 核心目标

把 AX 从“内部原型”变成“别人拿到就能试”的工程产品。

### 这一阶段要补齐的东西

- 最小标准库面：
  - `io`
  - `fs`
  - `path`
  - `env`
  - `process`
  - `string`
- 更清晰的项目模板和示例工程。
- 更稳定的安装、构建、benchmark、repair 使用说明。
- 更完整的 cookbook、示例集、坏例子语料。
- 更成熟的 adapter 规范和 IDE 集成方式。

### 本阶段完成标志

- 新用户不需要口头指导，也能安装、运行、看懂错误、做一次 repair benchmark。
- 外部接口文档稳定，不会一周一变。

## 阶段 7：自举准备

### 状态

- 状态：`长期目标`

### 核心目标

开始验证 AX 能不能用自己逐步重写自己的编译器核心。

### 推荐顺序

1. 先用 AX 重写 lexer / parser 的子集。
2. 再做语义模块的局部替换。
3. 再做交叉校验与双实现比对。
4. 最后尝试自编译与稳定复现。

### 进入这一步之前必须满足

- 前端、语义、后端、文档、benchmark 都已经比较稳定。
- 项目不会因为“为了自举而自举”把主线打乱。

## 当前最现实的执行顺序

如果只看“现在开始下一步该做什么”，推荐固定成下面这个顺序：

1. 继续收口当前外部接口。
   - 稳 diagnostics、AI schema、AST/HIR/MIR/build 快照。
2. 继续加固 AI 修复协议。
   - 稳 rule 触发、session version、规则覆盖测试。
3. 补运行期结构化诊断。
   - 让 `run` 也能进入修复链。
4. 只补当前最影响实践的语法缺口。
   - 优先处理切片、空数组字面量策略这类问题。
5. 把 benchmark 做成固定节奏。
   - 固定数据集、固定对比、固定报告。
6. benchmark 给正信号后，再扩语法面。
7. 语法面收口后，再推进原生后端。
8. 原生后端稳定后，再做标准库和产品化。
9. 最后再做自举。

## 当前最该盯住的 P0 / P1 / P2

### P0：必须先做，不做会影响整条主线

- `check/json/ai` 契约稳定
- AI 规则触发稳定化
- AI session state 版本化
- 高价值错误码规则覆盖测试
- `run` 结构化错误输出
- 文档与实现同步
- benchmark 固定输入输出协议

### P1：P0 稳住后马上做

- 切片
- 空数组字面量策略
- 更多高频类型错误与 unsupported guidance
- 示例与坏例子语料补齐
- benchmark 自动化报告更完整

### P2：有了 benchmark 证据再推进

- `match`
- 更丰富类型表面
- 原生后端
- 更完整标准库
- 对外交付与自举准备

## 现在不要做的事

当前阶段不该分散精力去做这些：

- GUI
- Web 框架
- async / await
- 宏系统
- 复杂泛型
- 包注册表
- 大而全标准库
- 多平台大规模铺开
- “统一所有 agent/workflow”的大叙事

这些东西不是永远不做，而是**现在做了会把 AX 最关键的差异化验证打散**。

## 这份规划怎么维护

- 每完成一部分实际工作，都要同步检查本文件是否需要更新。
- 如果完成的工作改变了“当前主线、当前阶段状态、下一步顺序、P0/P1/P2 判断”，必须在同一轮提交里同步修改本文件。
- 如果只是实现细节推进、但不改变整体路线，也至少要确认本文件仍然准确，不能长期放任它过时。
- 当阶段切换时，优先更新本文件的“当前主线”和“当前执行顺序”。
- 当原则、边界、成功条件变化时，先改 [`PLAN.md`](C:/Users/xiaoy/Desktop/A语言/AX/PLAN.md)。
- 当某项具体任务开始、完成或延后时，改 [`WORKLIST.md`](C:/Users/xiaoy/Desktop/A语言/AX/WORKLIST.md)。

## 当前一句话结论

AX 接下来最该做的，不是继续发散加语法，而是先把**现有原型收口成稳定接口**，再把**AI 修复闭环做硬**，然后用 **benchmark** 决定后续要不要继续扩语言面和原生后端。
