# AX 缺失语法清单

> 阅读提示：这份文件只回答“AX 现在还缺哪些语法，以及它们该按什么优先级补”。  
> 它不是路线总表；总体阶段顺序请看 [`C:\Users\xiaoy\Desktop\A语言\AX\规划.md`](C:\Users\xiaoy\Desktop\A语言\AX\规划.md)，当前施工项请看 [`C:\Users\xiaoy\Desktop\A语言\AX\WORKLIST.md`](C:\Users\xiaoy\Desktop\A语言\AX\WORKLIST.md)。

## 先说明白：哪些已经不算“缺失”

下面这些能力已经进入主链，不应再重复记成“还没做”：

- `module / import`：已有最小第一版
- `continue;`：已支持
- `match`：已有最小第一版
- 固定长度数组 / slice / struct / enum / `for` / `break`
- `AX.toml + sources` 项目模式

也就是说，AX 现在不是“基础语法一片空白”，而是已经过了第一轮原型可用期，开始进入“继续补哪些更值钱”的阶段。

## 统一原则

AX 后续补语法时，默认遵守这条纪律：

- 不能只补 parser
- 每个语法都应尽量一次补齐：
  - lexer / token
  - AST / parser
  - semantic
  - HIR / MIR 或 lowering
  - interpreter 或运行路径
  - diagnostics / AI rule
  - formatter
  - docs / examples / tests

一句话：**缺失语法可以补，但不能用“只看起来支持”来充数。**

## P1：现在最值得补的缺失语法

这批语法的共同特点是：

- 对真实工具代码表达力提升明显
- 对 AI 生成稳定性帮助大
- 改动面可控
- 不会立刻把 AX 拉进“通用语言大工程”泥潭

### 1. `&&` / `||`

- 价值：最高优先级的基础缺口之一
- 原因：
  - 工具代码里的条件组合非常常见
  - 没有它们会导致 if 嵌套过多、可读性下降
  - 对 AI 来说也是高频模式
- 预期要求：
  - 有短路语义
  - 语义层要求两边都为 `bool`
  - 解释器与 lowering 行为一致

### 2. `%`

- 价值：基础运算缺口
- 原因：
  - 成本不高
  - 在索引分组、分桶、奇偶判断、轮转逻辑里常见
- 预期要求：
  - 类型规则与 `i32` 算术一致
  - runtime error 边界与除零规则统一考虑

### 3. `for in`

- 价值：真实工具代码可写性会明显提升
- 原因：
  - 现在 `for (init; cond; step)` 能用，但写遍历仍然比较硬
  - 如果后面要让 AX 多写目录/文本/集合处理，遍历语法迟早要补
- 第一版建议：
  - 只支持数组 / slice
  - 暂不同时扩到所有容器

### 4. `match` v2

- 价值：高，但要控制节奏
- 当前现状：
  - 已有第一版 `match` 语句
  - 只支持 `bool` / `i32` / enum / `_`
- 现在还缺：
  - 表达式形态 `match`
  - 简单绑定模式
  - 更实用的分支写法
- 不建议第一步就上：
  - 解构
  - guard
  - 多模式合并

### 5. payload enum

- 价值：数据建模能力会提升一个台阶
- 原因：
  - 现在 enum 更像“无负载标签”
  - 如果后面想建模状态、结果、协议消息，这一层迟早要补
- 但优先级略低于 `&& / ||` 和 `for in`

## P2：应该做，但不该现在立刻做大的

这批语法很重要，但一旦开始，复杂度会明显上升。

### 6. `pub`

- 价值：模块边界真正开始成型时会需要
- 当前不急的原因：
  - 现在 `module / import` 还是最小模式
  - 可见性系统不是当前工具风格样例的主阻塞点

### 7. `import alias`

- 价值：中等
- 作用：
  - 减少长模块路径书写压力
  - 改善大型项目可读性
- 当前不急的原因：
  - 目前模块规模还不够大

### 8. wildcard import

- 价值：有，但风险也高
- 风险：
  - 会削弱 AX 当前“显式、确定”的风格
- 建议：
  - 如果以后做，也应非常克制

### 9. method / `impl`

- 价值：会让 API 设计更自然
- 风险：
  - 会引入方法解析、命名规则、调用 lowering、diagnostics 一整套复杂度
- 当前判断：
  - 应放在模块和数据建模更成熟之后

### 10. 泛型函数

- 价值：高
- 风险：
  - 类型推导、实例化、报错、AI rule 都会变复杂
- 当前判断：
  - 不是现在这轮最值钱的补法

## P3：明确后置的大语法

这批不是“永远不做”，而是“现在做会显著打乱主线”。

### 11. 泛型类型系统深化

- 例如：
  - 泛型 struct
  - 泛型 enum
  - 更复杂约束

### 12. trait / interface / impl 体系

- 这是通用语言的大工程节点
- 一旦开做，就不是小补丁，而是语言能力重构

### 13. 闭包 / lambda

- 很有用
- 但当前 AX 还没到需要引入这层抽象的阶段

### 14. `async / await`

- 当前明确后置
- 会把运行模型、诊断模型、AI 反馈模型都拉复杂

### 15. 异常

- 当前明确后置
- 与 AX 现在追求的确定性风格也不完全一致

### 16. 宏系统

- 当前明确后置
- 这是“语言成熟期”才值得碰的表面积

## 当前缺口排序

按“现在补了最值”的顺序，我建议是：

1. `&&` / `||`
2. `%`
3. `for in`（先只做数组 / slice）
4. `match` v2（先补一小步，不要全开）
5. payload enum
6. `pub`
7. `import alias`
8. method / `impl`
9. 泛型函数
10. trait / async / macro / exception

## 现在不建议做的误区

- 不要为了“像完整语言”而一口气上泛型、trait、async、宏
- 不要只在 parser 里把关键字接进去，就宣布“语法已支持”
- 不要同时开太多大语法面，导致 benchmark、AI 反馈、宿主能力主线被稀释

## 一句话结论

AX 现在真正缺的，不是“什么都没有”，而是：

> **还缺一批高价值、低失控风险的实用语法。**

下一批最合理的顺序，不是“先做最酷的”，而是：

> **先做 `&& / ||`、`%`、`for in` 这类能立刻提升工具代码表达力的能力，再考虑更大的类型系统和抽象系统。**
