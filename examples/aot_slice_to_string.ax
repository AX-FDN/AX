fn main() -> i32 {
    let values: [i32; 5] = [1, 2, 3, 4, 5];
    let flags: [bool; 3] = [true, false, true];
    let middle: [i32] = values[1:4];

    println(middle);
    println(flags[0:2]);

    return string_len(to_string(middle)) + string_len(to_string(flags[0:2]));
}
