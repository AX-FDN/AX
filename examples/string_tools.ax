fn main() -> i32 {
    let prefix: string = "AX";
    let title: string = prefix + " report";
    let details: string = title + " ready";
    println(details);
    println(string_len(details));
    return string_len(prefix);
}
