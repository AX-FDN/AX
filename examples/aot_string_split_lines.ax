fn main() -> i32 {
    let lines: [string] = string_split_lines("alpha\nbeta\ngamma\n");
    println(lines[1]);
    if (len(lines) == 3 && string_len(lines[0]) == 5 && string_len(lines[1]) == 4 && string_len(lines[2]) == 5) {
        return 17;
    }
    return 1;
}
