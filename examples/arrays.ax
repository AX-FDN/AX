fn edge_sum(values: [i32; 3]) -> i32 {
    return values[0] + values[2];
}

fn main() -> i32 {
    let mut values: [i32; 3] = [1, 2, 3];
    values[1] = edge_sum(values);
    println(values);
    println(values[1]);
    return 0;
}
