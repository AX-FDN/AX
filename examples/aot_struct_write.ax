struct Point {
    x: i32,
    y: i32,
}

fn main() -> i32 {
    let mut point: Point = Point { x: 2, y: 5 };
    point.y = point.x + 10;
    point.x = point.y - 3;
    println(point.x + point.y);
    return point.x;
}
